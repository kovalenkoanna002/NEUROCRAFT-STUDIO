import React from "react";
import { Article, Title, Lead, H2, P, Ul, Formula, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const ContextWindowArticle: React.FC = () => (
  <Article>
    <Title>Контекстное окно</Title>
    <Lead>
      Контекстное окно — это максимальное количество токенов, которое языковая
      модель может одновременно держать «в поле зрения»: и запрос, и порождаемый
      ответ. Размер окна ограничен, и причина кроется в устройстве механизма
      внимания.
    </Lead>

    <H2>Что такое контекстное окно</H2>
    <P>
      Текст разбивается на токены — слова или части слов. Контекстное окно задаёт,
      сколько таких токенов модель учитывает за один проход. Всё, что выходит за
      пределы окна, модель «не видит»: ранние части длинного диалога могут
      выпадать из рассмотрения.
    </P>

    <H2>Квадратичная сложность внимания</H2>
    <P>
      В трансформере механизм самовнимания сопоставляет каждый токен с каждым.
      Значит, при длине последовательности n число попарных взаимодействий растёт
      как n². Это и время вычислений, и объём памяти.
    </P>
    <Formula>стоимость внимания ∝ n²</Formula>
    <P>
      Поэтому удвоение длины контекста увеличивает затраты примерно вчетверо —
      именно квадратичный рост делает очень длинные окна дорогими.
    </P>

    <H2>Почему окно ограничено</H2>
    <Ul
      items={[
        "Память: матрица внимания размера n × n быстро переполняет видеопамять.",
        "Скорость: вычисления растут квадратично по длине входа.",
        "Качество: на очень длинных последовательностях модели сложнее удерживать связи.",
      ]}
    />
    <Callout variant="warn">
      Если разговор длиннее окна, ранний контекст приходится отбрасывать или
      сжимать — модель может «забыть» сказанное в начале.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default ContextWindowArticle;
