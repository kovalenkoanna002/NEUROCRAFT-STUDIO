import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ObjectDetectionArticle: React.FC = () => (
  <Article>
    <Title>Детекция объектов (YOLO, R-CNN)</Title>
    <Lead>
      Детекция объектов отвечает не только на вопрос «что на изображении», но и
      «где»: модель находит объекты и обводит каждый ограничивающей рамкой
      (bounding box) с меткой класса. Это шаг вперёд по сравнению с обычной
      классификацией, где объект на картинке один.
    </Lead>

    <H2>Что выдаёт детектор</H2>
    <P>
      Для каждого найденного объекта модель предсказывает координаты рамки,
      класс и уверенность (confidence). Одно изображение может содержать
      произвольное число объектов разных классов.
    </P>
    <Ul
      items={[
        "Координаты рамки: центр (x, y), ширина и высота.",
        "Метка класса с вероятностью.",
        "Score уверенности, что в рамке действительно есть объект.",
      ]}
    />

    <H2>Двухэтапные детекторы: семейство R-CNN</H2>
    <P>
      Двухэтапные методы сначала предлагают области-кандидаты, а затем
      классифицируют каждую.
    </P>
    <H3>R-CNN → Fast R-CNN → Faster R-CNN</H3>
    <Ul
      items={[
        "R-CNN: внешний алгоритм предлагает регионы, CNN классифицирует каждый отдельно — медленно.",
        "Fast R-CNN: признаки изображения считаются один раз, регионы берутся с общей карты.",
        "Faster R-CNN: предложение регионов делает обучаемая сеть RPN — всё работает единым конвейером.",
      ]}
    />
    <P>
      Двухэтапные детекторы обычно точнее, но медленнее. Их основа — глубокие
      свёрточные сети; остаточные связи ResNet (He и др., 2016) часто служат
      backbone для извлечения <Term id="feature-map">признаков</Term>.
    </P>

    <H2>Одноэтапные детекторы: YOLO</H2>
    <P>
      Redmon и др. (2016) предложили подход YOLO (You Only Look Once): сеть за
      один проход делит изображение на сетку и сразу предсказывает рамки и классы
      для всех ячеек. Детекция превращается в единую задачу регрессии.
    </P>
    <Ul
      items={[
        "Один проход сети вместо тысяч кандидатов — очень высокая скорость.",
        "Работает в реальном времени, подходит для видео.",
        "Видит изображение целиком, поэтому реже путает фон с объектом.",
      ]}
    />
    <Callout variant="info">
      Деление на одно- и двухэтапные детекторы — это компромисс «скорость против
      точности»: YOLO быстрее, Faster R-CNN традиционно точнее на мелких
      объектах.
    </Callout>

    <H2>Метрика IoU и подавление дубликатов</H2>
    <P>
      Качество рамки оценивают через IoU — отношение площади пересечения
      предсказанной и истинной рамок к площади их объединения:
    </P>
    <Formula>IoU = площадь(пересечение) / площадь(объединение)</Formula>
    <P>
      Чтобы убрать многократные рамки вокруг одного объекта, применяют
      Non-Maximum Suppression (NMS): оставляют рамку с максимальной уверенностью и
      удаляют сильно перекрывающиеся с ней.
    </P>

    <H2>Метрика качества mAP</H2>
    <P>
      Итоговое качество детектора измеряют через mAP (mean Average Precision) —
      усреднённую по классам площадь под кривой точность–полнота при заданном
      пороге IoU.
    </P>

    <H2>Где применяется</H2>
    <Ul
      items={[
        "Беспилотный транспорт: распознавание машин, пешеходов, знаков.",
        "Видеонаблюдение и контроль доступа.",
        "Промышленный контроль качества на конвейере.",
        "Медицинская визуализация: поиск патологий на снимках.",
      ]}
    />

    <Sources refs={[REFS.redmon2016, REFS.he2016, REFS.nikolenko]} />
  </Article>
);

export default ObjectDetectionArticle;
