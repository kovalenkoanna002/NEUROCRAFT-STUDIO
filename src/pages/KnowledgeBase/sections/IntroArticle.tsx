import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const IntroArticle: React.FC = () => (
  <Article>
    <Title>Что такое нейронные сети</Title>
    <Lead>
      Искусственная нейронная сеть — это математическая модель, вдохновлённая
      работой мозга. Она учится решать задачи на примерах, а не по жёстко
      заданным правилам.
    </Lead>

    <H2>Нейрон — базовый элемент</H2>
    <P>
      Сеть состоит из <Term id="neuron">нейронов</Term>. Каждый нейрон берёт
      входные числа, умножает их на <Term id="weight">веса</Term>, складывает,
      добавляет <Term id="bias">смещение</Term> и пропускает результат через{" "}
      <Term id="activation">функцию активации</Term>:
    </P>
    <Formula>y = f( w₁·x₁ + w₂·x₂ + … + wₙ·xₙ + b )</Formula>

    <H2>Почему сеть «учится»</H2>
    <P>
      Веса и смещения сначала случайны. Сеть делает предсказание, сравнивает его
      с правильным ответом через <Term id="loss">функцию потерь</Term> и
      корректирует параметры так, чтобы ошибка уменьшалась. Этот процесс
      повторяется много <Term id="epoch">эпох</Term>.
    </P>

    <H2>Из чего складывается сеть</H2>
    <Ul
      items={[
        "Входной слой — принимает признаки объекта (пиксели, числа, токены).",
        "Скрытые слои — извлекают всё более сложные закономерности.",
        "Выходной слой — выдаёт ответ: класс, число или распределение вероятностей.",
      ]}
    />

    <Callout variant="tip">
      Глубина (число слоёв) и ширина (число нейронов) определяют «ёмкость» сети.
      Больше — не всегда лучше: растёт риск{" "}
      <Term id="overfitting">переобучения</Term>.
    </Callout>

    <H2>Где применяются</H2>
    <Ul
      items={[
        "Компьютерное зрение: распознавание объектов, сегментация, генерация изображений.",
        "Обработка языка: перевод, чат-боты, суммаризация.",
        "Звук: распознавание речи, синтез голоса.",
        "Рекомендации, прогнозирование, медицина, робототехника.",
      ]}
    />

    <H2>Три парадигмы обучения</H2>
    <Ul
      items={[
        "Обучение с учителем (supervised): данные размечены — есть пары «вход → правильный ответ». Так решают классификацию и регрессию.",
        "Обучение без учителя (unsupervised): меток нет; сеть ищет структуру сама — кластеризация, снижение размерности, автокодировщики.",
        "Обучение с подкреплением (reinforcement): агент действует в среде и учится на вознаграждении — игры, робототехника.",
      ]}
    />

    <H2>Как проходит один проход (forward pass)</H2>
    <P>
      Данные проходят сквозь слои слева направо: каждый слой превращает входной
      вектор в новый набор признаков. Рассмотрим крошечный пример — нейрон с
      двумя входами x₁ = 1, x₂ = 2, весами w₁ = 0.5, w₂ = −1 и смещением b = 0.5:
    </P>
    <Formula>z = 0.5·1 + (−1)·2 + 0.5 = −1 → ReLU(−1) = 0</Formula>
    <P>
      Нейрон «не сработал». Меняя веса в процессе обучения, сеть настраивает,
      какие комбинации входов активируют какие нейроны.
    </P>

    <H2>Глубина и ширина</H2>
    <P>
      Ширина — число нейронов в слое, глубина — число слоёв. Глубокие сети
      выстраивают иерархию признаков: ранние слои улавливают простые паттерны,
      поздние комбинируют их в сложные. Именно «глубина» дала название
      глубокому обучению.
    </P>
    <Callout variant="warn">
      Увеличение размера сети повышает её ёмкость, но без достаточного объёма
      данных ведёт к <Term id="overfitting">переобучению</Term>. Размер модели
      подбирают под объём и сложность данных.
    </Callout>

    <Sources refs={[REFS.nikolenko, REFS.goodfellow, REFS.kubsuProgram]} />
  </Article>
);

export default IntroArticle;
