import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const WeightInitArticle: React.FC = () => (
  <Article>
    <Title>Инициализация весов</Title>
    <Lead>
      Перед обучением веса нужно задать начальными значениями. От выбора
      инициализации напрямую зависит, сойдётся ли сеть и как быстро.
    </Lead>

    <H2>Почему нельзя нулями</H2>
    <P>
      Если все веса равны (например, нулю), все нейроны слоя считают одно и то же
      и получают одинаковые градиенты — они навсегда остаются идентичными. Это
      называют проблемой симметрии. Поэтому веса инициализируют случайно.
    </P>

    <H2>Почему важен масштаб</H2>
    <Ul
      items={[
        "Слишком большие веса → активации и градиенты «взрываются».",
        "Слишком маленькие → сигнал затухает по мере прохождения слоёв.",
        "Цель — сохранить разумную дисперсию сигнала от слоя к слою.",
      ]}
    />

    <H2>Популярные схемы</H2>
    <Ul
      items={[
        "Xavier (Glorot) — для sigmoid/tanh: масштаб зависит от числа входов и выходов.",
        "He (Kaiming) — для ReLU: учитывает, что половина активаций обнуляется.",
        "Смещения обычно инициализируют нулями.",
      ]}
    />

    <Callout variant="tip">
      В современных фреймворках разумная инициализация задана по умолчанию для
      каждого типа слоя — менять её нужно лишь в особых случаях.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default WeightInitArticle;
