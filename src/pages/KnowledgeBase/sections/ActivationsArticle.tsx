import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import ActivationPlot from "../interactive/ActivationPlot";
import { REFS } from "../references";

const ActivationsArticle: React.FC = () => (
  <Article>
    <Title>Функции активации</Title>
    <Lead>
      Функция активации добавляет в сеть нелинейность. Без неё стопка
      полносвязных слоёв схлопывается в один линейный слой и не может выучить
      сложные зависимости.
    </Lead>

    <H2>Поиграйте с графиком</H2>
    <P>
      Выберите функцию и подвигайте ползунок <i>x</i> — точка покажет значение{" "}
      <i>f(x)</i>.
    </P>
    <ActivationPlot />

    <H2>Основные функции</H2>

    <H3>ReLU</H3>
    <P>
      <Term id="relu">ReLU</Term> — стандартный выбор для скрытых слоёв: проста и
      быстра, не насыщается для положительных значений. Её эффективность для
      глубоких сетей показали Nair и Hinton (2010).
    </P>
    <Formula>f(x) = max(0, x)</Formula>
    <Callout variant="warn">
      «Умирающие ReLU»: если вход постоянно отрицателен, нейрон всегда выдаёт 0 и
      перестаёт обучаться. Помогает Leaky ReLU или ELU.
    </Callout>

    <H3>Sigmoid</H3>
    <P>
      <Term id="sigmoid">Sigmoid</Term> сжимает значение в диапазон (0, 1) —
      удобно для вероятности в бинарной классификации.
    </P>
    <Formula>σ(x) = 1 / (1 + e⁻ˣ)</Formula>

    <H3>Tanh</H3>
    <P>
      <Term id="tanh">Tanh</Term> похож на sigmoid, но центрирован вокруг нуля,
      диапазон (−1, 1).
    </P>

    <H3>Softmax</H3>
    <P>
      <Term id="softmax">Softmax</Term> используется на выходе многоклассовой
      сети: превращает вектор в распределение вероятностей, сумма которых равна
      единице.
    </P>

    <Callout variant="tip">
      Практическое правило: ReLU (или его варианты) в скрытых слоях, sigmoid для
      бинарного выхода, softmax — для многоклассового.
    </Callout>

    <H2>Почему важна производная</H2>
    <P>
      При обучении градиент «протекает» назад, перемножаясь на производные
      активаций (правило цепочки). Если производная мала, градиент затухает, и
      ранние слои почти не обучаются — это проблема{" "}
      <b>исчезающего градиента</b>.
    </P>
    <Ul
      items={[
        "Sigmoid и tanh насыщаются: на краях производная близка к нулю → затухание градиента в глубоких сетях.",
        "ReLU имеет производную 1 для x > 0 → градиент не затухает, обучение идёт быстрее.",
        "Leaky ReLU/ELU дают ненулевой наклон при x < 0, спасая «умирающие» нейроны.",
      ]}
    />

    <H2>Сравнение по диапазону значений</H2>
    <Ul
      items={[
        "ReLU: [0, +∞) — разреженные активации, быстрый расчёт.",
        "Sigmoid: (0, 1) — интерпретируется как вероятность.",
        "Tanh: (−1, 1) — центрирован вокруг нуля, удобен для скрытых слоёв RNN.",
        "Softmax: распределение по классам с суммой 1 — только для выходного слоя.",
      ]}
    />

    <Callout variant="warn">
      Softmax не ставят в скрытых слоях: он нормирует значения относительно друг
      друга и предназначен именно для выхода классификатора.
    </Callout>

    <Sources refs={[REFS.nairHinton2010, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default ActivationsArticle;
