import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const Seq2SeqArticle: React.FC = () => (
  <Article>
    <Title>Seq2Seq (encoder–decoder)</Title>
    <Lead>
      Seq2Seq — архитектура для преобразования одной последовательности в другую:
      она кодирует входную последовательность в вектор состояния, а затем
      декодирует из него выходную. Классический пример — машинный перевод.
    </Lead>

    <H2>Кодировщик и декодировщик</H2>
    <P>
      Кодировщик (encoder) читает вход элемент за элементом и сжимает его в
      вектор состояния — компактное представление всего смысла последовательности.
      Декодировщик (decoder) пошагово порождает выход, опираясь на это состояние и
      на уже сгенерированные элементы.
    </P>
    <Ul
      items={[
        "Длины входа и выхода могут различаться (перевод, реферирование).",
        "Исторически кодировщик и декодировщик строились на рекуррентных сетях, в том числе LSTM.",
        "При генерации каждый предсказанный токен подаётся обратно на следующий шаг.",
      ]}
    />

    <H2>Узкое место и внимание</H2>
    <P>
      Сжатие длинной последовательности в один вектор — узкое место: информация о
      начале входа теряется. Механизм внимания снял это ограничение, позволив
      декодеру обращаться к нужным частям входа, а затем привёл к появлению
      трансформеров, полностью построенных на внимании.
    </P>

    <Callout variant="info">
      Современный машинный перевод и многие задачи генерации текста используют
      encoder–decoder трансформеры — прямое развитие идеи Seq2Seq.
    </Callout>

    <Sources refs={[REFS.lstm1997, REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default Seq2SeqArticle;
