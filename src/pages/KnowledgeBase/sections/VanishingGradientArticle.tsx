import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  InlineCode,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const VanishingGradientArticle: React.FC = () => (
  <Article>
    <Title>Затухающий и взрывающийся градиент</Title>
    <Lead>
      Затухающий (vanishing) и взрывающийся (exploding) градиент — это две
      классические проблемы обучения глубоких сетей. При обратном
      распространении ошибки сигнал, проходя через множество слоёв, может либо
      постепенно угасать почти до нуля, либо неконтролируемо расти. И то и другое
      мешает сети учиться.
    </Lead>

    <H2>Почему градиент затухает</H2>
    <P>
      При <Term id="backprop">обратном распространении</Term> производная функции
      потерь по весам ранних слоёв вычисляется как произведение множества
      множителей — по одному на каждый слой (цепное правило). Если эти множители
      по модулю меньше единицы, их произведение по мере углубления стремительно
      приближается к нулю.
    </P>
    <Formula>∂L/∂w₁ = δ_n · w_n · σ′(z_n) · … · w₂ · σ′(z₂) · x</Formula>
    <P>
      Главный виновник — производная функции активации. У{" "}
      <Term id="sigmoid">сигмоиды</Term> и <Term id="tanh">гиперболического
      тангенса</Term> производная в зоне насыщения близка к нулю: максимум
      производной сигмоиды всего 0,25. Перемножив десяток таких чисел, мы
      получаем исчезающе малое значение.
    </P>
    <Ul
      items={[
        "Ранние слои почти не получают сигнала об ошибке и обучаются крайне медленно.",
        "Сеть фактически использует только последние слои, а первые остаются «замороженными».",
        "Чем глубже сеть, тем сильнее эффект — поэтому до конца 2000-х очень глубокие сети почти не обучались.",
      ]}
    />

    <H2>Почему градиент взрывается</H2>
    <P>
      Обратная ситуация: если множители по модулю больше единицы, их
      произведение растёт экспоненциально. <Term id="gradient">Градиент</Term>{" "}
      становится огромным, шаг <Term id="gradient-descent">градиентного
      спуска</Term> выбрасывает веса далеко за разумные пределы, и значение{" "}
      <Term id="loss">функции потерь</Term> превращается в{" "}
      <InlineCode>NaN</InlineCode>.
    </P>
    <Callout variant="warn">
      Взрывающийся градиент особенно характерен для рекуррентных сетей на длинных
      последовательностях, где одни и те же веса применяются десятки раз подряд.
    </Callout>

    <H2>Функции активации против затухания</H2>
    <P>
      Замена насыщающихся активаций на <Term id="relu">ReLU</Term> стала одним из
      ключевых шагов вперёд. Производная ReLU равна единице для всех
      положительных входов, поэтому она не «душит» градиент при прохождении через
      слои (Nair, Hinton, 2010).
    </P>
    <H3>Сравнение производных</H3>
    <Ul
      items={[
        "Сигмоида: производная ≤ 0,25, быстро уходит в ноль при больших |z|.",
        "Tanh: производная ≤ 1, но тоже насыщается на краях.",
        "ReLU: производная равна 1 для z > 0 и 0 для z < 0 — градиент проходит без ослабления.",
        "Leaky ReLU и его варианты дают небольшой наклон при z < 0, чтобы нейроны не «умирали».",
      ]}
    />

    <H2>Остаточные связи и нормализация</H2>
    <P>
      Радикальное решение предложили остаточные сети ResNet (He и др., 2016).
      Вместо того чтобы заставлять слой выучивать преобразование{" "}
      <Formula>H(x)</Formula> с нуля, его учат выучивать поправку{" "}
      <Formula>F(x)</Formula>, а выход формируют как сумму{" "}
      <Formula>y = F(x) + x</Formula>.
    </P>
    <P>
      Этот «короткий путь» (skip connection) даёт градиенту прямой канал к ранним
      слоям: даже если <Formula>F(x)</Formula> почти не пропускает сигнал,
      слагаемое <Formula>x</Formula> переносит градиент без изменений. Именно так
      удалось обучить сети глубиной более ста слоёв.
    </P>
    <Ul
      items={[
        "Остаточные связи (ResNet) — добавляют тождественный путь для градиента.",
        "Batch Normalization — стабилизирует распределение активаций между слоями.",
        "Грамотная инициализация весов (He/Xavier) — сохраняет масштаб сигнала на старте.",
      ]}
    />

    <H2>Как бороться на практике</H2>
    <P>
      Против взрывающегося градиента простой и надёжный приём — обрезка градиента
      (gradient clipping): если норма градиента превышает порог, её
      масштабируют обратно.
    </P>
    <CodeBlock
      language="python"
      code={`import torch.nn as nn

# Обрезка градиента по норме перед шагом оптимизатора
loss.backward()
nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
optimizer.step()`}
    />
    <Callout variant="tip">
      Если потери внезапно стали NaN — почти наверняка взорвался градиент.
      Уменьшите <Term id="learning-rate">скорость обучения</Term>, добавьте
      clipping и проверьте инициализацию весов.
    </Callout>
    <P>
      Сочетание ReLU-подобных активаций, остаточных связей, нормализации и
      аккуратной инициализации сделало обучение очень глубоких сетей рутиной —
      хотя ещё пятнадцать лет назад это казалось почти невозможным (Goodfellow и
      др.; Николенко и др.).
    </P>

    <Sources
      refs={[REFS.he2016, REFS.nairHinton2010, REFS.goodfellow, REFS.nikolenko]}
    />
  </Article>
);

export default VanishingGradientArticle;
