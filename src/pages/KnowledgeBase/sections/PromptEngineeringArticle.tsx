import React from "react";
import { Article, Title, Lead, H2, H3, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const PromptEngineeringArticle: React.FC = () => (
  <Article>
    <Title>Промпт-инжиниринг</Title>
    <Lead>
      Промпт-инжиниринг — это искусство формулировать запросы к языковой модели
      так, чтобы получать точные и полезные ответы. Поскольку трансформерные
      модели чувствительны к формулировке, продуманный промпт сильно влияет на
      результат.
    </Lead>

    <H2>Zero-shot и few-shot</H2>
    <H3>Zero-shot</H3>
    <P>
      В режиме zero-shot модели дают только инструкцию без примеров. Например:
      «Определи тональность отзыва». Модель опирается на знания, полученные при
      предобучении.
    </P>
    <H3>Few-shot</H3>
    <P>
      В режиме few-shot в запрос добавляют несколько примеров пар «вход — ответ».
      Видя образцы, модель лучше понимает формат и характер задачи, что обычно
      повышает точность.
    </P>

    <H2>Chain-of-thought</H2>
    <P>
      Приём «цепочка рассуждений» (chain-of-thought) просит модель рассуждать
      пошагово, прежде чем дать итоговый ответ. Для задач с логикой и
      арифметикой это заметно повышает качество, потому что модель не пытается
      выдать ответ сразу, а раскладывает решение на шаги.
    </P>
    <Ul
      items={[
        "Добавьте фразу вроде «рассуждай шаг за шагом».",
        "Просите показывать промежуточные выводы.",
        "Финальный ответ выделяйте отдельно для удобства разбора.",
      ]}
    />

    <H2>Роль системного промпта</H2>
    <P>
      Системный промпт задаёт общую роль, стиль и ограничения модели на весь
      диалог: например, «ты вежливый помощник, отвечай кратко и по-русски». Он
      действует как постоянный контекст, на фоне которого обрабатываются
      пользовательские запросы.
    </P>
    <Callout variant="tip">
      Чёткая инструкция, формат ответа и пара примеров часто работают лучше, чем
      длинный, но расплывчатый запрос.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default PromptEngineeringArticle;
