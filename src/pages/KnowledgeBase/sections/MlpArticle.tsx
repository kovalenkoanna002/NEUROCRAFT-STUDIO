import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  BuilderLink,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const KERAS = `from tensorflow import keras
from tensorflow.keras import layers

model = keras.Sequential([
    keras.Input(shape=(4,)),
    layers.Dense(16, activation='relu'),
    layers.Dense(8, activation='relu'),
    layers.Dense(3, activation='softmax'),
])`;

const MlpArticle: React.FC = () => (
  <Article>
    <Title>Полносвязные сети (Dense / MLP)</Title>
    <Lead>
      Многослойный перцептрон (MLP) — самая базовая архитектура: слои{" "}
      <Term id="dense">полносвязных</Term> нейронов, где каждый нейрон соединён
      со всеми выходами предыдущего слоя.
    </Lead>

    <H2>Как устроен слой</H2>
    <P>
      Полносвязный слой — это матричное умножение входа на матрицу весов плюс
      смещение, а затем функция активации:
    </P>
    <Formula>h = f( W · x + b )</Formula>
    <P>
      Если у слоя <i>n</i> входов и <i>m</i> нейронов, матрица весов имеет размер{" "}
      <i>m × n</i>, то есть <i>m·n + m</i> обучаемых параметров.
    </P>

    <H2>Пример на Keras</H2>
    <CodeBlock code={KERAS} language="python" />
    <P>
      Эта сеть принимает 4 признака, имеет два скрытых слоя (16 и 8 нейронов с{" "}
      <Term id="relu">ReLU</Term>) и выход на 3 класса с{" "}
      <Term id="softmax">softmax</Term>.
    </P>

    <BuilderLink type="perceptron" />

    <H2>Сильные и слабые стороны</H2>
    <Ul
      items={[
        "✅ Универсальны: теоретически приближают любую функцию.",
        "✅ Просты в реализации и понимании.",
        "❌ Не учитывают структуру данных (порядок, соседство пикселей).",
        "❌ Много параметров на больших входах — отсюда CNN и RNN.",
      ]}
    />

    <Callout variant="info">
      Для изображений вместо MLP используют{" "}
      <Term id="convolution">свёрточные</Term> сети — они учитывают
      пространственные связи и требуют меньше параметров.
    </Callout>

    <H2>Сколько у сети параметров</H2>
    <P>
      Возьмём сеть из примера выше: 4 → 16 → 8 → 3. Параметры считаются послойно
      как «входы × нейроны + смещения»:
    </P>
    <Formula>
      (4·16 + 16) + (16·8 + 8) + (8·3 + 3) = 80 + 136 + 27 = 243
    </Formula>
    <P>
      Уже на крошечной сети — сотни параметров. Для изображения 28×28 один
      полносвязный слой на 128 нейронов имеет 784·128 + 128 ≈ 100 тысяч весов —
      отсюда и потребность в свёрточных сетях.
    </P>

    <H2>Как обучается MLP</H2>
    <Ul
      items={[
        "1. Прямой проход: вход проходит сквозь слои, сеть выдаёт предсказание.",
        "2. Расчёт потерь: предсказание сравнивается с правильным ответом.",
        "3. Обратное распространение: вычисляются градиенты потерь по всем весам.",
        "4. Шаг оптимизатора: веса сдвигаются против градиента.",
        "5. Повтор на следующих батчах и эпохах, пока потеря не перестанет падать.",
      ]}
    />

    <H2>Когда выбирать MLP</H2>
    <Ul
      items={[
        "✅ Табличные данные без выраженной пространственной/временной структуры.",
        "✅ Как «голова»-классификатор поверх свёрточных или трансформерных признаков.",
        "❌ Изображения, текст, звук — там выигрывают специализированные архитектуры.",
      ]}
    />

    <Sources
      refs={[REFS.rumelhart1986, REFS.goodfellow, REFS.nikolenko, REFS.kubsuProgram]}
    />
  </Article>
);

export default MlpArticle;
