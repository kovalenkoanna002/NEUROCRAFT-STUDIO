import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const ClassImbalanceArticle: React.FC = () => (
  <Article>
    <Title>Дисбаланс классов</Title>
    <Lead>
      Дисбаланс классов возникает, когда одни классы встречаются в данных намного чаще других.
      Типичные примеры — обнаружение мошенничества или редких заболеваний, где интересующий класс
      составляет малую долю выборки. Без специальных мер модель склонна игнорировать редкий класс.
    </Lead>

    <H2>В чём проблема</H2>
    <P>
      Если 99% примеров принадлежат одному классу, модель может достичь 99% accuracy, всегда
      предсказывая большинство, но при этом совсем не находить редкий класс. Поэтому при
      дисбалансе нельзя ориентироваться на accuracy.
    </P>
    <Callout variant="warn">
      Оценивайте качество по precision, recall, F1 или ROC-AUC отдельно для редкого класса —
      эти метрики показывают, действительно ли модель его находит.
    </Callout>

    <H2>Балансировка данных</H2>
    <P>
      Один подход — изменить состав обучающей выборки, чтобы классы стали более равными.
    </P>
    <Ul
      items={[
        "Oversampling — увеличение числа примеров редкого класса (дублирование или генерация новых).",
        "Undersampling — сокращение числа примеров частого класса.",
        "Комбинированные методы сочетают оба приёма.",
      ]}
    />
    <P>
      У каждого способа есть риск: oversampling может приводить к переобучению на повторяющихся
      примерах, а undersampling — к потере полезной информации из частого класса.
    </P>

    <H2>Веса классов</H2>
    <P>
      Альтернатива — оставить данные как есть, но изменить функцию потерь: ошибкам на редком
      классе назначают больший вес, чтобы модель уделяла ему больше внимания. Многие реализации
      алгоритмов в scikit-learn поддерживают параметр весов классов напрямую.
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.sklearn]} />
  </Article>
);

export default ClassImbalanceArticle;
