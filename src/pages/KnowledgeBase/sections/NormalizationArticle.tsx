import React from "react";
import { Article, Title, Lead, H2, P, Ul, Formula, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const NormalizationArticle: React.FC = () => (
  <Article>
    <Title>Batch и Layer Normalization</Title>
    <Lead>
      Нормализация активаций внутри сети ускоряет и стабилизирует обучение глубоких моделей.
      Она уменьшает зависимость каждого слоя от изменения распределений на выходе предыдущих
      слоёв и позволяет использовать большую скорость обучения.
    </Lead>

    <H2>Batch Normalization</H2>
    <P>
      Batch Normalization нормализует активации по мини-батчу: для каждого признака вычисляются
      среднее и дисперсия по примерам батча, после чего значения приводятся к нулевому среднему
      и единичной дисперсии.
    </P>
    <Formula>x̂ = (x − μ) / √(σ² + ε)</Formula>
    <P>
      Затем применяются обучаемые параметры масштаба γ и сдвига β, чтобы сеть могла при
      необходимости восстановить исходный масштаб: y = γ·x̂ + β. Малое ε добавляется для
      численной устойчивости. На этапе вывода используются накопленные за обучение скользящие
      средние μ и σ², поэтому результат не зависит от состава батча.
    </P>
    <Callout variant="tip">
      Batch Normalization снижает проблему «внутреннего ковариационного сдвига» и часто
      позволяет обучать сети без тщательной инициализации весов.
    </Callout>

    <H2>Layer Normalization</H2>
    <P>
      Layer Normalization нормализует не по батчу, а по признакам одного примера: среднее и
      дисперсия считаются по всем нейронам слоя для каждого объекта отдельно. Поэтому метод не
      зависит от размера батча и одинаково работает при обучении и выводе.
    </P>
    <Ul
      items={[
        "Batch Norm — по примерам в батче для каждого признака; чувствителен к размеру батча.",
        "Layer Norm — по признакам внутри одного примера; не зависит от батча.",
        "Layer Norm удобна для рекуррентных сетей и трансформеров, где батч может быть малым.",
      ]}
    />

    <H2>Когда что применять</H2>
    <P>
      Batch Normalization обычно используют в свёрточных сетях с достаточно большими батчами.
      Layer Normalization предпочтительна в обработке последовательностей и в трансформерах,
      где длина и состав данных меняются. Оба метода добавляют обучаемые параметры γ и β и
      обычно ставятся между линейным преобразованием и функцией активации.
    </P>

    <Sources refs={[REFS.batchnorm2015, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default NormalizationArticle;
