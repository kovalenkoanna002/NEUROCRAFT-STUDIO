import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const MoeArticle: React.FC = () => (
  <Article>
    <Title>Mixture of Experts (MoE)</Title>
    <Lead>
      Mixture of Experts — приём, при котором сеть состоит из множества
      «экспертов» и маршрутизатора, выбирающего, какие из них обработают
      конкретный вход. Это позволяет наращивать число параметров без роста
      вычислений на каждом примере.
    </Lead>

    <H2>Эксперты и маршрутизатор</H2>
    <P>
      Эксперты — это параллельные подсети (обычно полносвязные блоки). Небольшая
      обучаемая сеть-маршрутизатор (gating) оценивает вход и выбирает, какие
      эксперты к нему применить. Их выходы взвешенно складываются.
    </P>
    <Ul
      items={[
        "Каждый эксперт специализируется на своём типе данных.",
        "Маршрутизатор выдаёт веса (через softmax) и обычно отбирает top-k экспертов.",
        "Балансировка нагрузки не даёт всему трафику уходить в немногие эксперты.",
      ]}
    />

    <H2>Разреженная активация</H2>
    <P>
      Для каждого токена активируется лишь малая доля экспертов — это разреженная
      (sparse) активация. Общее число параметров модели огромно, но фактический
      объём вычислений на токен остаётся умеренным.
    </P>

    <Callout variant="info">
      MoE — один из способов масштабирования больших языковых моделей: сеть
      хранит десятки и сотни миллиардов параметров, активируя лишь часть из них.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default MoeArticle;
