import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ForwardPassArticle: React.FC = () => (
  <Article>
    <Title>Прямой проход (forward pass)</Title>
    <Lead>
      Прямой проход — это вычисление предсказания: данные проходят сквозь слои
      от входа к выходу, превращаясь на каждом слое в новый набор признаков.
    </Lead>

    <H2>Послойное вычисление</H2>
    <P>
      Каждый слой выполняет линейное преобразование и нелинейную активацию:
    </P>
    <Formula>h⁽ˡ⁾ = f( W⁽ˡ⁾ · h⁽ˡ⁻¹⁾ + b⁽ˡ⁾ )</Formula>
    <P>
      Выход одного слоя становится входом следующего, пока не получится итоговое
      предсказание ŷ.
    </P>

    <H2>Что происходит по шагам</H2>
    <Ul
      items={[
        "Вход подаётся в первый слой.",
        "Каждый слой считает взвешенную сумму и применяет активацию.",
        "Последний слой выдаёт ответ: класс, число или распределение вероятностей.",
        "Предсказание сравнивается с правильным ответом через функцию потерь.",
      ]}
    />

    <Callout variant="tip">
      Во время прямого прохода фреймворк запоминает промежуточные значения — они
      понадобятся для <Term id="backprop">обратного распространения</Term> при
      вычислении градиентов.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default ForwardPassArticle;
