import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const DataPreparationArticle: React.FC = () => (
  <Article>
    <Title>Предобработка данных</Title>
    <Lead>
      Качество модели в первую очередь определяется качеством данных. Перед
      обучением данные приводят к виду, удобному для сети.
    </Lead>

    <H2>Разделение выборки</H2>
    <Ul
      items={[
        "Обучающая (train) — на ней сеть подбирает веса.",
        "Валидационная (validation) — для подбора гиперпараметров и контроля переобучения.",
        "Тестовая (test) — финальная честная оценка качества.",
      ]}
    />
    <Callout variant="warn">
      Тестовую выборку нельзя использовать при настройке модели — иначе оценка
      качества будет завышенной.
    </Callout>

    <H2>Масштабирование признаков</H2>
    <Ul
      items={[
        "Нормализация: приведение к диапазону [0, 1].",
        "Стандартизация: нулевое среднее и единичная дисперсия.",
        "Без масштабирования градиентный спуск сходится медленнее.",
      ]}
    />

    <H2>Кодирование и аугментация</H2>
    <Ul
      items={[
        "Категориальные признаки кодируют (one-hot, embedding).",
        "Аугментация изображений (повороты, отражения, сдвиги) увеличивает разнообразие данных.",
        "Балансировка классов помогает при перекосе в данных.",
      ]}
    />

    <Callout variant="tip">
      Правило практики: «garbage in — garbage out». Часто улучшение данных даёт
      больший прирост, чем усложнение модели.
    </Callout>

    <H2>Работа с пропусками и выбросами</H2>
    <Ul
      items={[
        "Пропуски заполняют средним/медианой, отдельной категорией или удаляют.",
        "Выбросы выявляют и при необходимости ограничивают (clipping) или удаляют.",
        "Важно: статистики (среднее, дисперсия) считают только по обучающей выборке.",
      ]}
    />
    <Callout variant="warn">
      Утечка данных (data leakage): если масштабировать или заполнять пропуски по
      всей выборке сразу, информация из теста «протекает» в обучение и оценка
      становится завышенной.
    </Callout>

    <H2>Дисбаланс классов</H2>
    <P>
      Когда один класс встречается редко, модель может научиться всегда выдавать
      большинство. Помогают взвешивание классов в функции потерь,
      передискретизация (oversampling/undersampling) и подходящие метрики (F1,
      ROC-AUC вместо accuracy).
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.kubsuProgram]} />
  </Article>
);

export default DataPreparationArticle;
