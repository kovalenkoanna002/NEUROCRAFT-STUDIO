import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ActiveLearningArticle: React.FC = () => (
  <Article>
    <Title>Активное обучение</Title>
    <Lead>
      Активное обучение (active learning) — это стратегия, при которой модель сама
      выбирает, какие примеры стоит разметить в первую очередь. Вместо случайной
      разметки всего подряд она запрашивает у человека только самые полезные
      объекты, экономя время и деньги.
    </Lead>

    <H2>Идея: разметка стоит дорого</H2>
    <P>
      Неразмеченных данных обычно много, а труд разметчика — дорог и медлен.
      Активное обучение исходит из того, что не все примеры одинаково ценны:
      разметка нескольких «трудных» объектов даёт больше, чем разметка сотен
      очевидных.
    </P>
    <Callout variant="info">
      Аналогия — хороший студент задаёт вопросы именно о том, чего не понимает, а
      не перечитывает то, что уже знает. Модель ведёт себя так же.
    </Callout>

    <H2>Цикл активного обучения</H2>
    <Ul
      items={[
        "Обучить модель на текущем небольшом размеченном наборе.",
        "Применить её к неразмеченному пулу и оценить «полезность» каждого примера.",
        "Выбрать самые информативные объекты и отдать их человеку на разметку (оракул).",
        "Добавить размеченные примеры в обучающую выборку и повторить.",
      ]}
    />
    <P>
      Цикл повторяют, пока качество не перестанет расти или не закончится бюджет
      на разметку.
    </P>

    <H2>Как выбирать примеры</H2>
    <H3>Неуверенность (uncertainty sampling)</H3>
    <P>
      Берут объекты, в которых модель меньше всего уверена — например, где
      вероятности классов почти равны. Это самая популярная стратегия.
    </P>
    <CodeBlock
      language="python"
      code={`import numpy as np

proba = model.predict_proba(X_pool)          # вероятности классов
uncertainty = 1 - proba.max(axis=1)          # 1 - уверенность
query_idx = np.argsort(uncertainty)[-10:]    # 10 самых неоднозначных
# эти примеры отправляем разметчику`}
    />
    <H3>Запрос по разногласию (query by committee)</H3>
    <P>
      Обучают несколько моделей и выбирают примеры, по которым они сильнее всего
      расходятся во мнениях — именно там новая разметка прояснит границу.
    </P>
    <H3>Ожидаемое изменение модели</H3>
    <P>
      Предпочитают примеры, которые сильнее всего изменят параметры модели, то
      есть дадут наибольший <Term id="gradient">градиент</Term> обновления.
    </P>

    <H2>Практика в scikit-learn и инструментах</H2>
    <P>
      На базе <Term id="dense">классических</Term> моделей из scikit-learn (Pedregosa и
      др.) активное обучение реализуется просто: метод{" "}
      <code>predict_proba</code> даёт оценки уверенности, а вокруг него строится
      цикл выбора. Для удобства есть специализированные библиотеки поверх
      scikit-learn.
    </P>
    <Ul
      items={[
        "Стартовать можно всего с нескольких десятков размеченных примеров.",
        "На каждой итерации размечают небольшую порцию самых полезных объектов.",
        "Качество отслеживают на отдельной отложенной выборке.",
      ]}
    />

    <H2>Ограничения</H2>
    <Callout variant="warn">
      Активное обучение смещает выборку в сторону «трудных» примеров. Если модель
      изначально систематически ошибается, она может раз за разом запрашивать не
      то, что нужно, и усиливать собственный перекос.
    </Callout>
    <Ul
      items={[
        "Нужна обратная связь от человека — это узкое место по скорости.",
        "Выбранные примеры нерепрезентативны, что усложняет оценку качества.",
        "Стратегия выбора сама требует настройки под конкретную задачу.",
      ]}
    />
    <P>
      Тем не менее при дорогой разметке активное обучение позволяет достичь того
      же качества при заметно меньшем числе размеченных примеров (Goodfellow и
      др.; Николенко и др.).
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.sklearn]} />
  </Article>
);

export default ActiveLearningArticle;
