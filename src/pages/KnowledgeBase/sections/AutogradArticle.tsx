import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const AutogradArticle: React.FC = () => (
  <Article>
    <Title>Автоматическое дифференцирование</Title>
    <Lead>
      Автоматическое дифференцирование (autograd) — техника точного вычисления
      производных программно заданных функций. Именно она избавляет от
      ручного вывода градиентов и делает возможным обучение глубоких сетей.
    </Lead>

    <H2>Три способа считать производные</H2>
    <Ul
      items={[
        "Символьное дифференцирование: выводит формулу производной (как в Wolfram). Точно, но формулы разрастаются («выражение взрывается»).",
        "Численное дифференцирование: оценивает производную через конечные разности. Просто, но неточно и дорого для многих параметров.",
        "Автоматическое дифференцирование: вычисляет точное значение производной, применяя правило цепочки к элементарным операциям. Точно и эффективно.",
      ]}
    />
    <P>
      Численная проверка через конечную разность всё же полезна для отладки:
    </P>
    <Formula>f′(x) ≈ ( f(x + h) − f(x − h) ) / (2h)</Formula>
    <Callout variant="info">
      Autograd — не символьное и не численное дифференцирование, а отдельный
      метод: он работает с вычислительным графом и даёт точные градиенты.
    </Callout>

    <H2>Основа — правило цепочки</H2>
    <P>
      Сеть — это композиция функций (слой за слоем). Производная композиции
      считается по правилу цепочки:
    </P>
    <Formula>d/dx f(g(x)) = f′(g(x)) · g′(x)</Formula>
    <P>
      Autograd разбивает любую функцию на элементарные операции (сложение,
      умножение, активации), для каждой из которых производная известна, и
      перемножает их по графу. Исторически алгоритм{" "}
      <Term id="backprop">обратного распространения</Term> ошибки (Румельхарт,
      Хинтон, Уильямс, 1986) — это и есть применение autograd к нейросети.
    </P>

    <H2>Прямой и обратный режимы</H2>
    <P>
      Автоматическое дифференцирование бывает двух режимов:
    </P>
    <Ul
      items={[
        "Прямой режим (forward mode): производные считаются вместе с прямым проходом. Эффективен, когда входов мало, а выходов много.",
        "Обратный режим (reverse mode): сначала прямой проход, затем градиенты текут от выхода ко входам. Эффективен, когда выход один (скаляр потерь), а параметров много — это случай нейросетей.",
      ]}
    />
    <Callout variant="tip">
      Обучение сети — это один скалярный выход (функция потерь) и миллионы
      параметров. Поэтому обратный режим оптимален: один обратный проход даёт
      градиенты по всем параметрам сразу.
    </Callout>

    <H2>Как это работает в коде</H2>
    <P>
      В PyTorch каждый тензор с <InlineCode>requires_grad=True</InlineCode>{" "}
      запоминает операции, в которых участвовал. Вызов{" "}
      <InlineCode>backward()</InlineCode> запускает обратный режим и записывает{" "}
      <Term id="gradient">градиенты</Term> в атрибут <InlineCode>.grad</InlineCode>.
    </P>
    <CodeBlock
      language="python"
      code={`import torch

x = torch.tensor([1.0, 2.0, 3.0], requires_grad=True)
y = (x ** 2).sum()        # y = x1² + x2² + x3²
y.backward()              # обратный проход

print(x.grad)             # ∂y/∂x = 2x → tensor([2., 4., 6.])`}
    />
    <H3>Управление графом</H3>
    <Ul
      items={[
        "torch.no_grad() — отключает построение графа (для инференса, экономит память).",
        "detach() — «отцепляет» тензор от графа, останавливая поток градиента.",
        "zero_grad() — обнуляет накопленные градиенты перед новым шагом (иначе они суммируются).",
      ]}
    />

    <H2>Связь с обучением</H2>
    <P>
      Autograd встроен в цикл обучения: посчитать потерю, получить градиенты,
      обновить веса оптимизатором.
    </P>
    <CodeBlock
      language="python"
      code={`for x, target in dataloader:
    optimizer.zero_grad()          # обнулить старые градиенты
    output = model(x)              # прямой проход (строится граф)
    loss = criterion(output, target)
    loss.backward()                # autograd: градиенты по всем весам
    optimizer.step()               # шаг градиентного спуска`}
    />
    <Formula>θ ← θ − η · ∇L(θ)</Formula>

    <H2>Почему это важно</H2>
    <Ul
      items={[
        "Освобождает от ручного вывода производных для сетей с миллионами параметров.",
        "Даёт точные градиенты (в отличие от конечных разностей).",
        "Работает для произвольных архитектур, собранных из дифференцируемых операций.",
        "Лежит в основе всех современных фреймворков глубокого обучения.",
      ]}
    />
    <Callout variant="warn">
      Autograd требует, чтобы все операции были дифференцируемыми. Недифференцируемые
      шаги (например, жёсткий выбор argmax) разрывают поток градиента — их обходят
      приёмами вроде «прямого пропуска градиента» (straight-through estimator).
    </Callout>

    <Sources refs={[REFS.rumelhart1986, REFS.goodfellow, REFS.pytorch]} />
  </Article>
);

export default AutogradArticle;
