import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const PretrainingArticle: React.FC = () => (
  <Article>
    <Title>Предобучение и дообучение</Title>
    <Lead>
      Современные языковые модели обучают в два этапа: сначала на огромных
      текстовых корпусах без разметки, а затем дообучают под конкретную задачу.
      Такой подход экономит данные и вычисления и лежит в основе трансформерных
      моделей.
    </Lead>

    <H2>Предобучение на больших корпусах</H2>
    <P>
      На этапе предобучения модель учат предсказывать пропущенные или следующие
      слова на гигантских неразмеченных коллекциях текста — книгах, статьях,
      веб-страницах. Разметка не нужна: целевые значения берутся из самого текста
      (самообучение). Так модель усваивает грамматику, факты и общие
      закономерности языка.
    </P>
    <Ul
      items={[
        "Данных нужны миллиарды слов, поэтому используют веб-масштабные корпуса.",
        "Архитектура — трансформер с механизмом внимания.",
        "Результат — универсальная базовая модель, ещё не заточенная под задачу.",
      ]}
    />

    <H2>Дообучение под задачу</H2>
    <P>
      Дообучение (fine-tuning) берёт уже предобученную модель и продолжает
      обучение на небольшом размеченном наборе для конкретной задачи: анализа
      тональности, ответов на вопросы, перевода. Поскольку базовые знания уже
      получены, для адаптации хватает гораздо меньшего объёма данных и времени.
    </P>

    <H2>Перенос обучения в NLP</H2>
    <P>
      Идея переноса обучения (transfer learning) — переиспользовать знания,
      полученные на одной задаче, для другой. В обработке естественного языка это
      стало стандартом: одна предобученная модель служит основой для десятков
      прикладных задач, что резко снижает порог входа.
    </P>
    <Callout variant="tip">
      Предобучение делают один раз и дорого, а дообучений под разные задачи может
      быть много, и каждое — дёшево.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default PretrainingArticle;
