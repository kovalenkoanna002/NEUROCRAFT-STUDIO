import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const VitArticle: React.FC = () => (
  <Article>
    <Title>Vision Transformer (ViT)</Title>
    <Lead>
      Vision Transformer переносит архитектуру трансформера, изначально созданную
      для текста, в задачи компьютерного зрения. Изображение представляется как
      последовательность токенов — патчей, а не обрабатывается свёртками.
    </Lead>

    <H2>Изображение как набор патчей</H2>
    <P>
      Картинка разбивается на сетку небольших квадратных фрагментов (например,
      16×16 пикселей). Каждый патч «разворачивается» в вектор и линейно
      проецируется в эмбеддинг — так получается последовательность токенов,
      аналогичная словам в предложении.
    </P>
    <Ul
      items={[
        "К эмбеддингам патчей добавляется позиционное кодирование.",
        "В начало добавляется специальный токен класса для итогового предсказания.",
        "Полученная последовательность подаётся в стандартный трансформер-энкодер.",
      ]}
    />

    <H2>Внимание вместо свёрток</H2>
    <P>
      Self-attention связывает каждый патч со всеми остальными, поэтому модель
      сразу учитывает глобальный контекст изображения, тогда как свёртки
      работают локально. У ViT нет встроенного предположения о локальности, и при
      достаточно больших данных это идёт на пользу качеству.
    </P>

    <Callout variant="info">
      ViT раскрывает потенциал при предобучении на очень больших наборах данных;
      на малых выборках свёрточные сети нередко оказываются точнее.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default VitArticle;
