import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const CudaArticle: React.FC = () => (
  <Article>
    <Title>CUDA и вычисления на GPU</Title>
    <Lead>
      Обучение нейросетей — это миллиарды операций над матрицами. Графические
      процессоры (GPU) выполняют такие операции тысячами параллельных потоков,
      ускоряя обучение в десятки и сотни раз по сравнению с CPU. CUDA —
      платформа NVIDIA, через которую фреймворки получают доступ к этой
      параллельной мощности.
    </Lead>

    <H2>Почему GPU быстрее CPU для нейросетей</H2>
    <P>
      Процессор (CPU) имеет несколько мощных ядер, оптимизированных под
      последовательные задачи с ветвлениями. GPU содержит тысячи простых ядер,
      рассчитанных на однотипные операции над большими массивами данных. Прямой
      и обратный проход сети сводятся к умножению матриц — идеальной задаче для
      такой архитектуры.
    </P>
    <Ul
      items={[
        "Параллелизм: одна операция применяется к тысячам элементов одновременно.",
        "Высокая пропускная способность памяти — важна для больших тензоров.",
        "Специализированные блоки (тензорные ядра) для матричных операций.",
        "Эффективность на батчах: чем больше пакет данных, тем выше загрузка GPU.",
      ]}
    />

    <H2>Что такое CUDA</H2>
    <P>
      CUDA (Compute Unified Device Architecture) — программно-аппаратная
      платформа NVIDIA для вычислений общего назначения на GPU. Поверх CUDA
      работают библиотеки cuDNN (примитивы глубокого обучения) и cuBLAS
      (линейная алгебра), которые и используют фреймворки вроде PyTorch и
      TensorFlow.
    </P>
    <Callout variant="info">
      Прикладному инженеру по машинному обучению редко приходится писать код на
      CUDA напрямую. Достаточно понимать, как переносить тензоры и модель на
      устройство и следить за загрузкой видеопамяти.
    </Callout>

    <H2>Работа с устройством в PyTorch</H2>
    <P>
      В PyTorch вычислительное устройство выбирается явно. Тензоры и модель
      нужно переносить на одно и то же устройство, иначе возникнет ошибка.
    </P>
    <CodeBlock
      code={`import torch
import torch.nn as nn

# Выбираем устройство: GPU, если доступен, иначе CPU
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print("Устройство:", device)

model = nn.Linear(784, 10).to(device)      # переносим модель
x = torch.randn(64, 784).to(device)        # и данные

y = model(x)                               # вычисление идёт на GPU
print(y.device)                            # cuda:0`}
    />
    <P>
      Метод <InlineCode>.to(device)</InlineCode> переносит данные в память
      выбранного устройства. Вызов <InlineCode>torch.cuda.is_available()</InlineCode>{" "}
      проверяет наличие совместимого GPU и установленных драйверов.
    </P>

    <H2>Видеопамять и размер батча</H2>
    <P>
      Главный ограниченный ресурс GPU — видеопамять (VRAM). В ней хранятся
      параметры модели, активации, градиенты и состояния оптимизатора. Если
      батч слишком большой, обучение упадёт с ошибкой нехватки памяти.
    </P>
    <Ul
      items={[
        "Уменьшение размера батча — первый способ снизить расход памяти.",
        "Накопление градиентов (gradient accumulation) имитирует большой батч малыми порциями.",
        "Смешанная точность (float16/bfloat16) сокращает память и ускоряет тензорные ядра.",
        "Освобождайте ненужные тензоры и не держите историю вычислений при инференсе.",
      ]}
    />
    <CodeBlock
      code={`# Инференс без построения графа вычислений экономит память
model.eval()
with torch.no_grad():
    preds = model(x)`}
    />

    <H2>Типичные ошибки и диагностика</H2>
    <Ul
      items={[
        "«CUDA out of memory» — уменьшите батч или включите смешанную точность.",
        "«Tensors on different devices» — модель и данные на разных устройствах.",
        "Низкая загрузка GPU — узкое место в загрузке данных, а не в вычислениях.",
        "Несовместимость версий драйвера, CUDA и фреймворка.",
      ]}
    />
    <Callout variant="tip">
      Команда <InlineCode>nvidia-smi</InlineCode> в терминале показывает текущую
      загрузку GPU и расход видеопамяти. Если загрузка низкая, а обучение идёт
      медленно — оптимизируйте загрузчик данных и увеличьте число воркеров.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.pytorch, REFS.nikolenko]} />
  </Article>
);

export default CudaArticle;
