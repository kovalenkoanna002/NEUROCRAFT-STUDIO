import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const CrossValidationArticle: React.FC = () => (
  <Article>
    <Title>Кросс-валидация</Title>
    <Lead>
      Кросс-валидация — способ честно оценить качество модели на ограниченных данных. Вместо
      одного разбиения на обучение и проверку данные многократно делят по-разному, что даёт
      более устойчивую оценку обобщающей способности.
    </Lead>

    <H2>Разбиение train/val/test</H2>
    <P>
      Базовый подход — разделить данные на три части. Обучающая выборка (train) служит для
      настройки весов, валидационная (val) — для подбора гиперпараметров и контроля
      переобучения, тестовая (test) — для финальной независимой оценки. Тестовую выборку нельзя
      использовать в процессе разработки, иначе оценка станет завышенной.
    </P>
    <Callout variant="warn">
      Если подбирать модель по тестовой выборке, она перестаёт быть независимой и итоговое
      качество окажется оптимистично завышенным.
    </Callout>

    <H2>k-fold кросс-валидация</H2>
    <P>
      При k-fold данные делят на k равных частей (фолдов). Модель обучают k раз: каждый раз одна
      часть служит для проверки, а остальные k−1 — для обучения. Итоговую метрику усредняют по
      всем k запускам. Это позволяет задействовать все данные и для обучения, и для проверки.
    </P>
    <Ul
      items={[
        "Типичные значения k — 5 или 10.",
        "Усреднение по фолдам снижает влияние удачного или неудачного разбиения.",
        "Цена — k-кратное увеличение времени обучения.",
      ]}
    />

    <H2>Стратифицированная версия</H2>
    <P>
      Stratified k-fold сохраняет в каждом фолде ту же пропорцию классов, что и во всём наборе
      данных. Это особенно важно при дисбалансе классов: иначе в каком-то фолде редкий класс
      может почти отсутствовать. В библиотеке scikit-learn для этого есть готовые инструменты
      разбиения.
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.sklearn]} />
  </Article>
);

export default CrossValidationArticle;
