import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const TensorsArticle: React.FC = () => (
  <Article>
    <Title>Тензоры и тензорные операции</Title>
    <Lead>
      Тензор — основная структура данных в глубоком обучении. Это многомерный
      массив чисел, на котором фреймворки выполняют все вычисления: от хранения
      картинок и весов до прямого и обратного проходов сети.
    </Lead>

    <H2>Что такое тензор</H2>
    <P>
      В контексте нейросетей тензор — это обобщение скаляра, вектора и матрицы на
      произвольное число измерений (осей). Число осей называют рангом (или
      порядком) тензора:
    </P>
    <Ul
      items={[
        "Ранг 0 — скаляр: одно число, например значение функции потерь.",
        "Ранг 1 — вектор: одномерный массив, например признаки одного объекта.",
        "Ранг 2 — матрица: таблица чисел, например матрица весов слоя.",
        "Ранг 3 и выше — собственно тензоры: цветное изображение (высота × ширина × каналы), батч изображений (батч × высота × ширина × каналы).",
      ]}
    />
    <Callout variant="info">
      Форма (shape) тензора — это кортеж размеров по каждой оси. Например, батч из
      32 RGB-картинок 28×28 имеет форму (32, 28, 28, 3).
    </Callout>

    <H2>Атрибуты тензора</H2>
    <Ul
      items={[
        "Форма (shape) — размеры по каждой оси.",
        "Тип данных (dtype) — обычно float32 для весов и активаций, int64 для индексов.",
        "Устройство (device) — CPU или GPU, на котором хранятся данные.",
        "Ранг (ndim) — число осей.",
      ]}
    />
    <CodeBlock
      language="python"
      code={`import torch

x = torch.zeros(32, 28, 28, 3)   # батч картинок
print(x.shape)   # torch.Size([32, 28, 28, 3])
print(x.ndim)    # 4
print(x.dtype)   # torch.float32`}
    />

    <H2>Основные операции</H2>
    <H3>Поэлементные операции</H3>
    <P>
      Сложение, умножение, применение функции активации выполняются поэлементно:
      каждый элемент результата зависит только от соответствующих элементов
      входов.
    </P>
    <Formula>(A + B)ᵢⱼ = Aᵢⱼ + Bᵢⱼ</Formula>

    <H3>Матричное умножение</H3>
    <P>
      Ключевая операция слоя сети — умножение матрицы входов на матрицу весов.
      Для матриц <Formula>A</Formula> размера <Formula>m×k</Formula> и{" "}
      <Formula>B</Formula> размера <Formula>k×n</Formula>:
    </P>
    <Formula>Cᵢⱼ = Σ_p Aᵢ_p · B_pⱼ</Formula>
    <CodeBlock
      language="python"
      code={`A = torch.randn(4, 3)
B = torch.randn(3, 5)
C = A @ B          # матричное умножение
print(C.shape)     # torch.Size([4, 5])`}
    />

    <H2>Broadcasting (трансляция форм)</H2>
    <P>
      Broadcasting позволяет выполнять операции над тензорами разной формы без
      явного копирования данных. Меньший тензор «растягивается» по недостающим
      осям. Например, прибавление вектора смещения <Formula>b</Formula> ко всем
      строкам матрицы:
    </P>
    <Formula>Y = X · W + b</Formula>
    <CodeBlock
      language="python"
      code={`X = torch.randn(32, 64)   # батч из 32, по 64 признака
W = torch.randn(64, 10)
b = torch.randn(10)
Y = X @ W + b             # b транслируется на все 32 строки
print(Y.shape)            # torch.Size([32, 10])`}
    />
    <Callout variant="tip">
      Правило broadcasting: оси сопоставляются справа налево; размеры совместимы,
      если равны или один из них равен 1.
    </Callout>

    <H2>Изменение формы</H2>
    <P>
      Часто нужно перестроить тензор, не меняя его данные: например, развернуть
      карту признаков перед полносвязным слоем.
    </P>
    <Ul
      items={[
        "reshape / view — меняют форму при том же числе элементов.",
        "flatten — сворачивает все оси (кроме батча) в одну.",
        "transpose / permute — переставляют оси местами.",
        "squeeze / unsqueeze — убирают или добавляют оси размера 1.",
      ]}
    />
    <CodeBlock
      language="python"
      code={`fmap = torch.randn(32, 64, 7, 7)   # карты признаков из свёрток
flat = fmap.flatten(start_dim=1)   # (32, 64*7*7)
print(flat.shape)                  # torch.Size([32, 3136])`}
    />

    <H2>Почему тензоры важны для нейросетей</H2>
    <Ul
      items={[
        "Все данные и параметры сети — тензоры; обучение сводится к тензорным операциям.",
        "Тензорные операции хорошо распараллеливаются на GPU, что ускоряет обучение в десятки раз.",
        "Фреймворки (PyTorch, TensorFlow) автоматически отслеживают операции над тензорами для вычисления градиентов.",
        "Работа батчами (первая ось — размер батча) позволяет обрабатывать много примеров одновременно.",
      ]}
    />
    <Callout variant="warn">
      Большинство ошибок при сборке сети — это несоответствие форм тензоров.
      Полезно мысленно (или через <InlineCode>print(x.shape)</InlineCode>)
      отслеживать форму на каждом шаге.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.pytorch]} />
  </Article>
);

export default TensorsArticle;
