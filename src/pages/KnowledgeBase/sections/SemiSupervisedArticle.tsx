import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const SemiSupervisedArticle: React.FC = () => (
  <Article>
    <Title>Частичное обучение (semi-supervised)</Title>
    <Lead>
      Частичное обучение (semi-supervised learning) занимает середину между
      обучением с учителем и без учителя. Модель учится на небольшом наборе
      размеченных примеров и одновременно использует большой объём неразмеченных
      данных, чтобы лучше понять структуру задачи.
    </Lead>

    <H2>Зачем это нужно</H2>
    <P>
      Разметка данных — самое дорогое и медленное звено в машинном обучении.
      Размечать миллионы изображений или текстов вручную почти невозможно, зато
      сырых, неразмеченных данных обычно сколько угодно. Частичное обучение
      позволяет извлечь пользу из этого «бесплатного» ресурса.
    </P>
    <Ul
      items={[
        "Медицина: размеченных снимков мало (нужен врач), но необработанных — десятки тысяч.",
        "Распознавание речи: транскрипция дорога, а аудиозаписей много.",
        "Модерация контента: разметить весь поток невозможно, размечают лишь выборку.",
      ]}
    />

    <H2>Главное предположение</H2>
    <P>
      Чтобы неразмеченные данные помогали, должно выполняться предположение о
      гладкости и кластерности: близкие в пространстве признаков объекты, как
      правило, относятся к одному классу, а граница между классами проходит по
      областям с малой плотностью данных (Goodfellow и др.).
    </P>
    <Callout variant="info">
      Неразмеченные точки «подсказывают» форму облаков данных. Зная всего по
      одному размеченному примеру в каждом кластере, модель может правильно
      разметить весь кластер.
    </Callout>

    <H2>Основные подходы</H2>
    <H3>Самообучение (self-training)</H3>
    <P>
      Модель обучают на размеченных данных, затем она предсказывает метки для
      неразмеченных. Самые уверенные предсказания (псевдометки) добавляют в
      обучающую выборку и повторяют процесс.
    </P>
    <CodeBlock
      language="python"
      code={`# Идея псевдоразметки (упрощённо)
model.fit(X_labeled, y_labeled)
proba = model.predict_proba(X_unlabeled)
confident = proba.max(axis=1) > 0.95          # уверенные примеры
X_new = X_unlabeled[confident]
y_new = proba[confident].argmax(axis=1)       # псевдометки
model.fit(concat(X_labeled, X_new), concat(y_labeled, y_new))`}
    />
    <H3>Согласованность (consistency regularization)</H3>
    <P>
      Модель должна давать одинаковый ответ для исходного и слегка искажённого
      примера (с шумом, аугментацией). Это требование добавляют в{" "}
      <Term id="loss">функцию потерь</Term> для неразмеченных данных.
    </P>
    <H3>Графовые методы</H3>
    <P>
      Объекты соединяют рёбрами по схожести и «распространяют» известные метки по
      графу на соседние неразмеченные вершины (label propagation).
    </P>

    <H2>Риски и ограничения</H2>
    <Callout variant="warn">
      Главная опасность — самоподтверждение ошибки: если модель уверенно
      присвоит неверную псевдометку, она будет учиться на собственной ошибке и
      усиливать её с каждой итерацией.
    </Callout>
    <Ul
      items={[
        "Если предположение о кластерности нарушено, неразмеченные данные могут даже навредить.",
        "Нужен порог уверенности и контроль качества псевдометок на отложенной выборке.",
        "Метод чувствителен к дисбалансу классов в неразмеченной части.",
      ]}
    />

    <H2>Место среди других парадигм</H2>
    <P>
      Частичное обучение тесно связано с самообучением (self-supervised) и{" "}
      <Term id="overfitting">регуляризацией</Term>: все они стремятся выжать
      максимум из ограниченной разметки. Сегодня его идеи лежат в основе многих
      систем компьютерного зрения и обработки речи, где разметка остаётся узким
      местом (Николенко и др.).
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default SemiSupervisedArticle;
