import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const FaceRecognitionArticle: React.FC = () => (
  <Article>
    <Title>Распознавание лиц</Title>
    <Lead>
      Распознавание лиц — задача определить или подтвердить личность человека по
      изображению лица. Современные системы строятся на свёрточных сетях, которые
      превращают лицо в компактный числовой вектор (эмбеддинг), а сравнение
      людей сводится к сравнению этих векторов.
    </Lead>

    <H2>Две разные задачи</H2>
    <Ul
      items={[
        "Верификация (1:1): «это тот же человек?» — сравнение двух лиц, как при разблокировке телефона.",
        "Идентификация (1:N): «кто это?» — поиск лица в базе из множества людей.",
      ]}
    />

    <H2>Конвейер распознавания</H2>
    <H3>1. Детекция лица</H3>
    <P>
      Сначала на кадре находят область лица (отдельная задача детекции). Затем по
      ключевым точкам (глаза, нос, углы рта) лицо выравнивают к стандартному
      положению.
    </P>
    <H3>2. Извлечение эмбеддинга</H3>
    <P>
      Свёрточная сеть переводит выровненное лицо в вектор фиксированной длины
      (например, 128 или 512 чисел). В роли backbone часто выступает ResNet (He и
      др., 2016), извлекающий <Term id="feature-map">признаки</Term> лица.
    </P>
    <H3>3. Сравнение векторов</H3>
    <P>
      Близость двух лиц измеряют расстоянием между их эмбеддингами. Если
      расстояние меньше порога — это один человек.
    </P>
    <Formula>d(a, b) = ‖ f(a) − f(b) ‖</Formula>

    <H2>Обучение: метрическое обучение</H2>
    <P>
      Ключевая идея — научить сеть так, чтобы лица одного человека давали близкие
      векторы, а разных людей — далёкие. Для этого применяют триплетную функцию
      потерь (triplet loss) с тройкой «якорь, позитив, негатив»:
    </P>
    <Formula>L = max(0, d(a, p) − d(a, n) + margin)</Formula>
    <Ul
      items={[
        "a (anchor) — эталонное лицо.",
        "p (positive) — другое фото того же человека: должно быть близко к a.",
        "n (negative) — лицо другого человека: должно быть далеко от a.",
      ]}
    />
    <Callout variant="info">
      Благодаря такому подходу систему не нужно переобучать ради нового человека:
      достаточно один раз вычислить его эмбеддинг и добавить в базу.
    </Callout>

    <H2>Метрики качества</H2>
    <Ul
      items={[
        "FAR (False Accept Rate) — доля ложных пропусков чужого как своего.",
        "FRR (False Reject Rate) — доля ложных отказов своему.",
        "Порог сравнения выбирают как компромисс между FAR и FRR под задачу.",
      ]}
    />

    <H2>Ограничения и этика</H2>
    <P>
      Точность падает при плохом освещении, поворотах головы, окклюзиях. Модели
      могут быть предвзяты, если в обучающих данных недостаточно представлены
      какие-то группы людей. Распознавание лиц затрагивает приватность и потому
      во многих юрисдикциях регулируется.
    </P>
    <Callout variant="warn">
      Системы уязвимы к спуфингу (фото, маски, дипфейки), поэтому в ответственных
      сценариях добавляют проверку «живости» (liveness detection).
    </Callout>

    <Sources refs={[REFS.he2016, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default FaceRecognitionArticle;
