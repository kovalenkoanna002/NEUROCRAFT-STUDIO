import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const InContextArticle: React.FC = () => (
  <Article>
    <Title>In-context learning</Title>
    <Lead>
      In-context learning — это способность языковой модели «учиться» на примерах,
      приведённых прямо в запросе, без обновления весов. Модель распознаёт образец
      из контекста и применяет его к новому входу.
    </Lead>

    <H2>Обучение без обновления весов</H2>
    <P>
      При обычном обучении модель меняет свои параметры по градиентам. При
      in-context learning ничего подобного не происходит: веса остаются
      неизменными. Модель просто использует приведённые в запросе примеры как
      часть входного контекста и по аналогии формирует ответ.
    </P>
    <Ul
      items={[
        "Веса модели не меняются — обучения в классическом смысле нет.",
        "Примеры действуют только в пределах одного запроса.",
        "Способность возникает сама собой после масштабного предобучения трансформера.",
      ]}
    />

    <H2>Как это выглядит</H2>
    <P>
      В запрос помещают несколько пар «вход — ответ», а затем новый вход без
      ответа. Модель, видя закономерность в примерах, продолжает её для нового
      случая. По сути это частный случай few-shot подхода, реализованный целиком
      через контекст.
    </P>

    <H2>Возможности и ограничения</H2>
    <P>
      In-context learning удобно тем, что не требует переобучения: достаточно
      изменить запрос. Однако объём примеров ограничен размером контекстного окна,
      а качество зависит от того, насколько примеры репрезентативны и понятны.
    </P>
    <Callout variant="info">
      Поскольку веса не обновляются, выученное «в контексте» исчезает, как только
      завершается запрос — это не постоянное знание модели.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default InContextArticle;
