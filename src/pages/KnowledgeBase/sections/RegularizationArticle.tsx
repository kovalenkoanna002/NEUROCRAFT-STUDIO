import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const RegularizationArticle: React.FC = () => (
  <Article>
    <Title>Регуляризация и предотвращение переобучения</Title>
    <Lead>
      <Term id="overfitting">Переобучение</Term> — когда модель отлично работает
      на обучающих данных, но плохо на новых. Регуляризация ограничивает
      «свободу» модели, улучшая обобщение.
    </Lead>

    <H2>Как распознать переобучение</H2>
    <P>
      Ошибка на обучении продолжает падать, а на валидации — растёт. Это сигнал
      остановить обучение или усилить регуляризацию.
    </P>

    <H2>Основные приёмы</H2>
    <H3>L1 и L2 регуляризация</H3>
    <P>
      Добавляют штраф за большие веса к функции потерь. L2 (weight decay)
      «сглаживает» веса, L1 — обнуляет часть из них (отбор признаков).
    </P>

    <H3>Dropout</H3>
    <P>
      <Term id="dropout">Dropout</Term> (Srivastava и др., 2014) случайно
      отключает часть нейронов на каждом шаге обучения, не давая сети полагаться
      на отдельные связи.
    </P>

    <H3>Batch Normalization</H3>
    <P>
      Нормализация активаций внутри сети (Ioffe и Szegedy, 2015) ускоряет
      обучение и оказывает регуляризирующий эффект.
    </P>

    <H3>Ранняя остановка и аугментация</H3>
    <Ul
      items={[
        "Early stopping — прекращаем обучение, когда ошибка на валидации перестаёт падать.",
        "Больше данных и аугментация — лучший способ борьбы с переобучением.",
      ]}
    />

    <Callout variant="tip">
      Регуляризацию подбирают по валидации: цель — минимальный разрыв между
      качеством на обучении и на новых данных.
    </Callout>

    <H2>Компромисс смещения и разброса</H2>
    <Ul
      items={[
        "Высокое смещение (underfitting): модель слишком проста, ошибается и на обучении.",
        "Высокий разброс (overfitting): модель слишком сложна, идеальна на обучении, плоха на новых данных.",
        "Цель — баланс: регуляризация снижает разброс ценой небольшого роста смещения.",
      ]}
    />

    <H2>L1 или L2 — что выбрать</H2>
    <Ul
      items={[
        "L2 (weight decay): равномерно уменьшает все веса, делает модель «плавнее». Универсальный выбор.",
        "L1: обнуляет часть весов, давая разреженность и неявный отбор признаков.",
        "Elastic Net: комбинация L1 и L2.",
      ]}
    />

    <Callout variant="info">
      Dropout обычно ставят между полносвязными слоями (типичная доля 0.2–0.5), а
      Batch Normalization — после линейного слоя до активации. Эти приёмы часто
      используют вместе.
    </Callout>

    <Sources
      refs={[REFS.dropout2014, REFS.batchnorm2015, REFS.goodfellow, REFS.nikolenko]}
    />
  </Article>
);

export default RegularizationArticle;
