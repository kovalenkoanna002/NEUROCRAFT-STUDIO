import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const AiAgentArticle: React.FC = () => (
  <Article>
    <Title>Что такое ИИ-агент</Title>
    <Lead>
      ИИ-агент — это система, которая не просто отвечает на один запрос, а
      самостоятельно действует ради достижения цели: воспринимает обстановку,
      рассуждает и предпринимает шаги, наблюдая за результатом.
    </Lead>

    <H2>Цикл «восприятие → рассуждение → действие»</H2>
    <P>
      В основе агента сегодня обычно лежит большая языковая модель (LLM),
      встроенная в цикл. Агент получает задачу, анализирует текущее состояние,
      планирует следующий шаг, выполняет его и смотрит на результат — и так
      повторяет, пока цель не достигнута.
    </P>
    <Ul
      items={[
        "Восприятие — агент читает входные данные: запрос, результаты прошлых действий, состояние среды.",
        "Рассуждение — модель планирует, что делать дальше, и выбирает подходящее действие.",
        "Действие — агент выполняет шаг (вызывает инструмент, пишет текст) и получает обратную связь.",
      ]}
    />

    <H2>Инструменты и память</H2>
    <P>
      Чтобы влиять на внешний мир, агенту нужны инструменты: поиск, калькулятор,
      доступ к API, запуск кода. Чтобы не терять контекст между шагами, агенту
      нужна память — короткая (история текущего диалога) и долгая (внешнее
      хранилище фактов и прошлых решений).
    </P>
    <Callout variant="info">
      Развитие автономных агентов поднимает вопросы безопасности и контроля:
      организации вроде OpenAI прямо фиксируют в своих принципах обязательства по
      безопасному развитию подобных систем.
    </Callout>

    <Sources refs={[REFS.openaiCharter, REFS.nikolenko]} />
  </Article>
);

export default AiAgentArticle;
