import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const KERAS = `from tensorflow.keras import layers, Sequential

model = Sequential([
    layers.Embedding(10000, 64),
    layers.LSTM(128),
    layers.Dense(1, activation='sigmoid'),
])`;

const RnnArticle: React.FC = () => (
  <Article>
    <Title>Рекуррентные сети (RNN, LSTM, GRU)</Title>
    <Lead>
      Рекуррентные сети предназначены для последовательностей: текста, временных
      рядов, звука. В отличие от MLP они имеют «память» — скрытое состояние,
      которое переносится от шага к шагу.
    </Lead>

    <H2>Идея рекуррентности</H2>
    <P>
      На каждом шаге сеть получает текущий элемент xₜ и предыдущее состояние
      hₜ₋₁, и вычисляет новое состояние:
    </P>
    <Formula>hₜ = f( W·xₜ + U·hₜ₋₁ + b )</Formula>
    <P>
      Так информация из прошлого влияет на обработку настоящего.
    </P>

    <H2>Проблема длинных зависимостей</H2>
    <P>
      У простых RNN градиент при обучении на длинных последовательностях
      затухает или взрывается — сеть «забывает» далёкий контекст.
    </P>

    <H2>LSTM и GRU</H2>
    <H3>LSTM</H3>
    <P>
      Long Short-Term Memory (Hochreiter и Schmidhuber, 1997) вводит ячейку
      памяти и вентили (gates), которые управляют тем, что запоминать, забывать и
      выдавать. Это решает проблему длинных зависимостей.
    </P>
    <H3>GRU</H3>
    <P>
      Gated Recurrent Unit (Cho и др., 2014) — упрощённый вариант с меньшим
      числом вентилей; часто работает не хуже LSTM, но быстрее.
    </P>

    <H2>Пример на Keras</H2>
    <CodeBlock code={KERAS} language="python" />

    <Callout variant="info">
      Сегодня в обработке языка рекуррентные сети во многом вытеснены{" "}
      трансформерами, но RNN/LSTM остаются актуальны для временных рядов и
      компактных моделей.
    </Callout>

    <H2>Вентили LSTM</H2>
    <Ul
      items={[
        "Forget gate — что забыть из памяти.",
        "Input gate — что записать в память.",
        "Output gate — что выдать наружу как скрытое состояние.",
      ]}
    />
    <P>
      Благодаря вентилям информация может «течь» по ячейке почти без изменений на
      сотни шагов — это и решает проблему длинных зависимостей.
    </P>

    <H2>Виды задач с последовательностями</H2>
    <Ul
      items={[
        "Один-ко-многим: описание изображения текстом.",
        "Многие-к-одному: классификация тональности отзыва.",
        "Многие-ко-многим: машинный перевод, разметка частей речи.",
      ]}
    />

    <H2>Двунаправленные сети и seq2seq</H2>
    <P>
      Двунаправленный RNN читает последовательность в обе стороны — полезно,
      когда важен и левый, и правый контекст. Архитектура encoder–decoder
      (seq2seq) кодирует вход в вектор состояния и декодирует его в выходную
      последовательность; именно она лежала в основе раннего нейронного перевода.
    </P>

    <Sources
      refs={[REFS.lstm1997, REFS.gru2014, REFS.goodfellow, REFS.nikolenko]}
    />
  </Article>
);

export default RnnArticle;
