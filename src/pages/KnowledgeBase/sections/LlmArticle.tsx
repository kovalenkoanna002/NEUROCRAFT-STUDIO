import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  Ul,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const LlmArticle: React.FC = () => (
  <Article>
    <Title>Большие языковые модели (LLM)</Title>
    <Lead>
      LLM (Large Language Model) — это трансформер, обученный предсказывать
      следующий токен на огромных текстовых корпусах. Масштаб (миллиарды
      параметров и токенов) даёт способность к рассуждению, переводу,
      программированию и диалогу.
    </Lead>

    <H2>Как работает в основе</H2>
    <Ul
      items={[
        "Текст разбивается на токены (части слов).",
        "Модель-трансформер предсказывает вероятность следующего токена.",
        "Генерация идёт авторегрессивно: токен за токеном.",
      ]}
    />

    <H2>Как обучают</H2>
    <Ul
      items={[
        "Предобучение (pretraining): self-supervised обучение на гигантских корпусах без разметки.",
        "Дообучение под инструкции (instruction tuning): модель учат следовать заданиям.",
        "RLHF — обучение с подкреплением на основе обратной связи людей для полезности и безопасности.",
      ]}
    />

    <H2>Ключевые понятия</H2>
    <Ul
      items={[
        "Контекстное окно — сколько токенов модель «видит» одновременно.",
        "Промпт-инжиниринг — формулировка запроса для нужного результата.",
        "In-context learning — обучение на примерах прямо в запросе (few-shot).",
        "RAG — подмешивание внешних знаний для точности и актуальности.",
      ]}
    />

    <Callout variant="warn">
      Галлюцинации: LLM может уверенно выдавать правдоподобную, но неверную
      информацию. Критичные факты нужно проверять, а в продакшене — заземлять
      ответы на источники (RAG, цитирование).
    </Callout>

    <Callout variant="info">
      Примеры LLM: GPT (OpenAI), Claude (Anthropic), GigaChat и YandexGPT
      (российские). Развитие LLM — одна из центральных тем конференции AI Journey.
    </Callout>

    <Sources
      refs={[REFS.transformer2017, REFS.openaiCharter, REFS.aiJourney, REFS.nikolenko]}
    />
  </Article>
);

export default LlmArticle;
