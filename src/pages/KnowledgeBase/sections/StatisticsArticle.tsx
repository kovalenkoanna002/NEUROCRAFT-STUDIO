import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const StatisticsArticle: React.FC = () => (
  <Article>
    <Title>Теория вероятностей и статистика</Title>
    <Lead>
      Машинное обучение работает с неопределённостью: данные шумны, а модель
      оценивает вероятности. Базовые понятия теории вероятностей лежат в основе
      функций потерь и метрик.
    </Lead>

    <H2>Ключевые понятия</H2>
    <Ul
      items={[
        "Случайная величина и распределение вероятностей.",
        "Математическое ожидание — среднее значение по распределению.",
        "Дисперсия — мера разброса.",
        "Условная вероятность P(y | x) — то, что предсказывает классификатор.",
      ]}
    />

    <H2>Softmax как распределение</H2>
    <P>
      Выход классификатора с <Term id="softmax">softmax</Term> — это
      распределение вероятностей по классам, сумма которых равна единице.
    </P>

    <H2>Кросс-энтропия и правдоподобие</H2>
    <P>
      Обучение классификатора эквивалентно максимизации правдоподобия (MLE), что
      сводится к минимизации <Term id="loss">кросс-энтропии</Term> между истинным
      и предсказанным распределениями:
    </P>
    <Formula>L = − Σ yᵢ · log(ŷᵢ)</Formula>

    <Callout variant="info">
      Многие функции потерь — это не произвольные формулы, а следствие
      вероятностной постановки задачи (метод максимального правдоподобия).
    </Callout>

    <H2>Полезные распределения</H2>
    <Ul
      items={[
        "Нормальное (гауссово) — шум, инициализация весов, латентное пространство VAE.",
        "Бернулли — бинарный исход, выход sigmoid.",
        "Категориальное — многоклассовый выход softmax.",
      ]}
    />

    <H2>Энтропия и KL-дивергенция</H2>
    <P>
      Энтропия измеряет неопределённость распределения, а KL-дивергенция —
      «расстояние» между двумя распределениями. Минимизация кросс-энтропии
      эквивалентна сближению предсказанного и истинного распределений.
    </P>

    <Callout variant="tip">
      Вероятностный взгляд объясняет, почему модели выдают не просто метку, а
      уверенность: это оценка условной вероятности P(класс | вход).
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.kubsuProgram]} />
  </Article>
);

export default StatisticsArticle;
