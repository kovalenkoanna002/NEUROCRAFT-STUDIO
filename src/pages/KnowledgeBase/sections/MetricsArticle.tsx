import React from "react";
import { Article, Title, Lead, H2, P, Ul, Formula, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const MetricsArticle: React.FC = () => (
  <Article>
    <Title>Метрики качества</Title>
    <Lead>
      Метрика — это число, которым измеряют качество модели на данных. Выбор метрики зависит от
      задачи: для классификации и регрессии используют разные показатели, и неверная метрика
      может скрыть реальные проблемы модели.
    </Lead>

    <H2>Метрики классификации</H2>
    <P>
      Accuracy (доля верных ответов) — самая простая метрика: число правильных предсказаний,
      делённое на общее число примеров. Однако accuracy обманчива при дисбалансе классов: если
      99% объектов относятся к одному классу, модель, всегда предсказывающая этот класс, даст
      99% точности, но будет бесполезной.
    </P>
    <Ul
      items={[
        "Precision (точность) — доля действительно положительных среди предсказанных положительными.",
        "Recall (полнота) — доля найденных положительных среди всех реально положительных.",
        "F1 — гармоническое среднее precision и recall, баланс между ними.",
        "ROC-AUC — площадь под ROC-кривой; качество ранжирования вне зависимости от порога.",
      ]}
    />
    <Formula>F1 = 2 · (precision · recall) / (precision + recall)</Formula>
    <Callout variant="warn">
      При сильном дисбалансе классов опирайтесь на precision, recall, F1 или ROC-AUC, а не на
      accuracy: высокая точность может быть достигнута тривиальным предсказанием большинства.
    </Callout>

    <H2>Метрики регрессии</H2>
    <P>
      Для задач предсказания числа используют ошибки между прогнозом и истинным значением.
    </P>
    <Formula>MAE = (1/n) · Σ|yᵢ − ŷᵢ|</Formula>
    <Formula>RMSE = √( (1/n) · Σ(yᵢ − ŷᵢ)² )</Formula>
    <P>
      MAE (средняя абсолютная ошибка) устойчива к выбросам и легко интерпретируется. RMSE
      (корень из среднеквадратичной ошибки) сильнее штрафует крупные промахи, поэтому полезна,
      когда большие ошибки особенно нежелательны.
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default MetricsArticle;
