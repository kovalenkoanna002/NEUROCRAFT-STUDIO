import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  Ul,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const RagArticle: React.FC = () => (
  <Article>
    <Title>RAG — генерация с дополнением из поиска</Title>
    <Lead>
      RAG (Retrieval-Augmented Generation) — подход, при котором языковая модель
      перед ответом находит релевантные документы во внешней базе и опирается на
      них. Предложен Lewis и др. (2020).
    </Lead>

    <H2>Зачем это нужно</H2>
    <Ul
      items={[
        "Знания модели «заморожены» на момент обучения — RAG даёт доступ к свежим данным.",
        "Снижает галлюцинации: ответ опирается на конкретные источники.",
        "Позволяет работать с приватными корпоративными документами без переобучения.",
      ]}
    />

    <H2>Как работает</H2>
    <Ul
      items={[
        "1. Запрос переводится в эмбеддинг.",
        "2. В векторной базе ищутся ближайшие по смыслу фрагменты документов.",
        "3. Найденные фрагменты добавляются в контекст модели.",
        "4. Модель генерирует ответ, опираясь на них, и может сослаться на источник.",
      ]}
    />

    <Callout variant="tip">
      RAG — стандартный способ построить «ассистента по документам». Качество
      зависит от поиска: хорошие эмбеддинги и чанкование документов важнее, чем
      размер самой LLM.
    </Callout>

    <Sources refs={[REFS.lewis2020, REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default RagArticle;
