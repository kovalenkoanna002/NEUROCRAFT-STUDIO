import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const HyperparametersArticle: React.FC = () => (
  <Article>
    <Title>Подбор гиперпараметров</Title>
    <Lead>
      Гиперпараметры — это настройки модели и алгоритма обучения, которые задаются до обучения и
      не подбираются градиентным спуском. От их выбора во многом зависит итоговое качество, и для
      их поиска применяют специальные стратегии.
    </Lead>

    <H2>Что такое гиперпараметр</H2>
    <P>
      В отличие от обучаемых параметров (весов и смещений), которые сеть находит сама в процессе
      обучения, гиперпараметры выбирает человек или алгоритм поиска. К ним относятся скорость
      обучения, размер батча, число слоёв и нейронов, коэффициент регуляризации, доля dropout.
    </P>
    <Callout variant="info">
      Гиперпараметры подбирают по валидационной выборке, а не по обучающей и не по тестовой —
      иначе оценка качества окажется завышенной.
    </Callout>

    <H2>Grid search</H2>
    <P>
      Поиск по сетке (grid search) перебирает все комбинации заранее заданных значений
      гиперпараметров. Метод прост и систематичен, но число комбинаций растёт экспоненциально с
      количеством параметров, поэтому он быстро становится дорогим.
    </P>
    <Ul
      items={[
        "Плюс — полный и предсказуемый перебор заданной сетки.",
        "Минус — комбинаторный взрыв при многих параметрах.",
        "Подходит, когда параметров мало, а диапазоны понятны.",
      ]}
    />

    <H2>Random search</H2>
    <P>
      Случайный поиск (random search) выбирает комбинации значений случайно из заданных
      распределений. При том же бюджете вычислений он часто находит более удачные настройки, чем
      grid search, потому что эффективнее исследует параметры, которые сильнее влияют на
      результат, не тратя ресурсы на малозначимые.
    </P>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default HyperparametersArticle;
