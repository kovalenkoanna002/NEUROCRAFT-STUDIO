import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  Ul,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const SupervisedArticle: React.FC = () => (
  <Article>
    <Title>Обучение с учителем</Title>
    <Lead>
      Самая распространённая парадигма: модель учится на размеченных данных — на
      парах «вход → правильный ответ». Цель — научиться предсказывать ответ для
      новых, ранее не виденных объектов.
    </Lead>

    <H2>Два типа задач</H2>
    <Ul
      items={[
        "Классификация — предсказание категории (спам/не спам, класс объекта).",
        "Регрессия — предсказание числа (цена, температура, спрос).",
      ]}
    />

    <H2>Как это работает</H2>
    <Ul
      items={[
        "Есть обучающая выборка с известными ответами (метками).",
        "Модель делает предсказание и сравнивает его с меткой через функцию потерь.",
        "Параметры корректируются, чтобы ошибка уменьшалась.",
        "Качество проверяют на отложенных данных, которых модель не видела.",
      ]}
    />

    <Callout variant="warn">
      Главное ограничение — нужна разметка, а она дорогая и трудоёмкая. Отсюда
      интерес к обучению без учителя и самообучению.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.kubsuProgram]} />
  </Article>
);

export default SupervisedArticle;
