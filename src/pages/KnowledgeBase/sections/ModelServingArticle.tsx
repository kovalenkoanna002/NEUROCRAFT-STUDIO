import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ModelServingArticle: React.FC = () => (
  <Article>
    <Title>Сервинг моделей</Title>
    <Lead>
      Обученная модель приносит пользу только тогда, когда к ней можно
      обратиться из приложения. Сервинг (model serving) — это развёртывание
      модели как сервиса, который принимает входные данные и возвращает
      предсказания. От обучения сервинг отличается тем, что во главе угла
      стоят скорость отклика, стабильность и масштабируемость.
    </Lead>

    <H2>Инференс против обучения</H2>
    <P>
      При обучении вычисляются градиенты и обновляются веса — это ресурсоёмко.
      При инференсе модель только выполняет прямой проход, без обратного
      распространения. Поэтому инференс можно сильно оптимизировать: отключить
      построение графа вычислений, перевести модель в режим оценки, применить
      квантизацию.
    </P>
    <Ul
      items={[
        "Латентность — время ответа на один запрос.",
        "Пропускная способность — число запросов в секунду.",
        "Стабильность — сервис не падает под нагрузкой.",
        "Версионирование — управляемая замена модели на новую.",
      ]}
    />

    <H2>Способы развёртывания</H2>
    <H3>REST API на своём сервере</H3>
    <P>
      Простой подход: обернуть модель в веб-сервис (например, на FastAPI или
      Flask) и принимать запросы по HTTP. Подходит для небольших нагрузок и
      прототипов.
    </P>
    <H3>Специализированные серверы</H3>
    <P>
      TensorFlow Serving и TorchServe — готовые серверы инференса с поддержкой
      пакетной обработки, версионирования и метрик. Они снимают с разработчика
      рутину написания инфраструктуры.
    </P>

    <H2>Пример: REST-сервис на FastAPI</H2>
    <CodeBlock
      code={`from fastapi import FastAPI
import torch

app = FastAPI()
model = torch.jit.load("model.pt")   # загружаем модель один раз при старте
model.eval()

@app.post("/predict")
def predict(features: list[float]):
    x = torch.tensor(features).unsqueeze(0)
    with torch.no_grad():
        logits = model(x)
        pred = int(logits.argmax(dim=1))
    return {"prediction": pred}`}
    />
    <Callout variant="info">
      Модель загружается один раз при запуске сервиса, а не на каждый запрос.
      Иначе латентность вырастет в разы из-за повторного чтения весов с диска.
    </Callout>

    <H2>Оптимизация инференса</H2>
    <Ul
      items={[
        "Батчинг: объединение нескольких запросов в один прямой проход.",
        "Квантизация: перевод весов из float32 в int8 ускоряет вычисления и уменьшает модель.",
        "Экспорт в формат ONNX для запуска в оптимизированных рантаймах.",
        "Кэширование результатов для часто повторяющихся запросов.",
      ]}
    />
    <CodeBlock
      code={`# Экспорт модели PyTorch в TorchScript для сервинга
scripted = torch.jit.script(model)
scripted.save("model.pt")`}
    />

    <H2>Контейнеризация и масштабирование</H2>
    <P>
      В продакшене сервис обычно упаковывают в контейнер (Docker) — так
      окружение становится воспроизводимым. Несколько копий сервиса запускают за
      балансировщиком нагрузки, а оркестратор (например, Kubernetes) добавляет и
      убирает реплики в зависимости от трафика.
    </P>
    <Callout variant="tip">
      Перед выкладкой обязательно проверьте сервис нагрузочным тестом: измерьте
      латентность и пропускную способность при реалистичном числе одновременных
      запросов. Это вскрывает узкие места до того, как их найдут пользователи.
    </Callout>

    <Sources refs={[REFS.tensorflow, REFS.pytorch, REFS.nikolenko]} />
  </Article>
);

export default ModelServingArticle;
