import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ExperimentTrackingArticle: React.FC = () => (
  <Article>
    <Title>MLflow и Weights &amp; Biases</Title>
    <Lead>
      В разработке моделей проводят десятки и сотни экспериментов с разными
      данными, архитектурами и гиперпараметрами. Без системного учёта легко
      забыть, какой запуск дал лучший результат и при каких настройках. Системы
      трекинга экспериментов — MLflow и Weights &amp; Biases — фиксируют каждый
      запуск, его параметры, метрики и артефакты.
    </Lead>

    <H2>Проблема невоспроизводимых экспериментов</H2>
    <P>
      Типичная ситуация: модель показала хорошую точность неделю назад, но какие
      именно гиперпараметры использовались — уже неясно. Ручные записи в
      блокноте теряются, а имена файлов вроде «model_final_v2_real» ничего не
      говорят. Трекинг решает эту проблему, автоматически связывая результат с
      условиями, в которых он получен.
    </P>
    <Ul
      items={[
        "Параметры: скорость обучения, размер батча, число эпох, архитектура.",
        "Метрики: потери и качество на обучении и валидации по шагам.",
        "Артефакты: веса модели, графики, файлы конфигурации.",
        "Версия кода и окружения, в котором запускался эксперимент.",
      ]}
    />

    <H2>MLflow</H2>
    <P>
      MLflow — открытая платформа для управления жизненным циклом моделей. Её
      модуль Tracking логирует параметры и метрики, а локальный или серверный
      интерфейс позволяет сравнивать запуски в таблице и на графиках.
    </P>
    <CodeBlock
      code={`import mlflow

with mlflow.start_run():
    mlflow.log_param("learning_rate", 0.001)
    mlflow.log_param("batch_size", 64)

    for epoch in range(epochs):
        train_loss = train_one_epoch(...)
        val_acc = evaluate(...)
        mlflow.log_metric("val_accuracy", val_acc, step=epoch)

    mlflow.log_artifact("model.pt")   # сохраняем веса`}
    />
    <Callout variant="info">
      MLflow можно запускать локально без отдельного сервера — логи сохраняются
      в каталог проекта, а интерфейс открывается командой
      <InlineCode>mlflow ui</InlineCode>.
    </Callout>

    <H2>Weights &amp; Biases</H2>
    <P>
      Weights &amp; Biases (W&amp;B) — облачный сервис трекинга с акцентом на
      наглядные дашборды и совместную работу. Логирование добавляется несколькими
      строками, а результаты автоматически появляются в веб-интерфейсе.
    </P>
    <CodeBlock
      code={`import wandb

wandb.init(project="digits", config={
    "learning_rate": 0.001,
    "batch_size": 64,
})

for epoch in range(epochs):
    train_loss = train_one_epoch(...)
    val_acc = evaluate(...)
    wandb.log({"val_accuracy": val_acc, "train_loss": train_loss})`}
    />
    <Ul
      items={[
        "Интерактивные дашборды и сравнение запусков в браузере.",
        "Sweeps — автоматический подбор гиперпараметров.",
        "Командная работа над проектами и обмен отчётами.",
        "Логирование изображений, аудио и таблиц предсказаний.",
      ]}
    />

    <H2>Что выбрать</H2>
    <P>
      Оба инструмента решают одну задачу, но с разным акцентом. MLflow
      разворачивается полностью на своей инфраструктуре, что важно при
      ограничениях на хранение данных. W&amp;B даёт более богатую визуализацию и
      командные функции «из коробки», но по умолчанию работает через облако.
    </P>
    <Callout variant="tip">
      Главный принцип: логировать эксперименты с самого начала проекта, а не
      когда уже что-то «потерялось». Минимальный набор — параметры, метрики
      валидации и ссылка на сохранённые веса.
    </Callout>

    <H2>Связь с воспроизводимостью</H2>
    <H3>Версии данных и кода</H3>
    <P>
      Полная воспроизводимость требует фиксировать не только гиперпараметры, но и
      версию датасета и кода. Трекеры обычно сохраняют хеш коммита, а версии
      данных контролируют отдельными инструментами.
    </P>
    <H3>Случайность</H3>
    <P>
      Инициализация весов и перемешивание данных зависят от генератора случайных
      чисел. Фиксация зерна (random seed) делает запуски сравнимыми.
    </P>

    <Sources refs={[REFS.nikolenko, REFS.goodfellow]} />
  </Article>
);

export default ExperimentTrackingArticle;
