import React from "react";
import { Article, Title, Lead, H2, P, Ul, Formula, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const AttentionArticle: React.FC = () => (
  <Article>
    <Title>Механизмы внимания</Title>
    <Lead>
      Внимание (attention) позволяет модели для каждого элемента
      последовательности решать, на какие другие элементы опираться сильнее. Это
      основа трансформеров и современных языковых моделей.
    </Lead>

    <H2>Запрос, ключ, значение</H2>
    <P>
      Каждый элемент представляется тремя векторами: запросом (Query), ключом
      (Key) и значением (Value). Запрос текущего элемента сравнивается с ключами
      всех элементов; чем выше совпадение, тем больший вес получает их значение.
    </P>
    <Ul
      items={[
        "Query — что ищет текущий элемент.",
        "Key — по чему элементы можно найти.",
        "Value — какую информацию они несут.",
      ]}
    />

    <H2>Формула внимания</H2>
    <P>
      Совпадения вычисляются как скалярные произведения запросов и ключей,
      масштабируются и превращаются в веса функцией softmax, после чего ими
      взвешиваются значения:
    </P>
    <Formula>Attention(Q, K, V) = softmax(Q·Kᵀ / √dₖ) · V</Formula>
    <P>
      Деление на √dₖ (корень из размерности ключей) стабилизирует масштаб
      скалярных произведений, чтобы softmax не уходил в насыщение.
    </P>

    <Callout variant="info">
      Self-attention применяет этот механизм внутри одной последовательности,
      связывая каждый токен со всеми остальными. На нём целиком построен
      трансформер.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default AttentionArticle;
