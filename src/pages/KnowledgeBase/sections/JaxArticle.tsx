import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const JaxArticle: React.FC = () => (
  <Article>
    <Title>JAX</Title>
    <Lead>
      JAX — библиотека Google для высокопроизводительных численных вычислений и
      машинного обучения. Её можно описать как «NumPy с автоматическим
      дифференцированием и компиляцией под ускорители». JAX особенно популярен в
      исследованиях, где важны скорость и гибкость экспериментов.
    </Lead>

    <H2>Идея: преобразования функций</H2>
    <P>
      В основе JAX лежит идея применять к обычным Python-функциям математические
      преобразования. Вы пишете чистую функцию, а JAX превращает её в
      производную, компилированную или векторизованную версию.
    </P>
    <Ul
      items={[
        "grad — автоматическое дифференцирование функции.",
        "jit — компиляция через XLA для ускорения на CPU, GPU и TPU.",
        "vmap — автоматическая векторизация (батчинг) без ручных циклов.",
        "pmap — распараллеливание по нескольким устройствам.",
      ]}
    />

    <H2>NumPy-совместимый интерфейс</H2>
    <P>
      Модуль <InlineCode>jax.numpy</InlineCode> повторяет API NumPy, поэтому
      привычный код переносится почти без изменений. Главное отличие — массивы
      JAX неизменяемы, а вычисления могут исполняться на GPU/TPU.
    </P>
    <CodeBlock
      language="python"
      code={`import jax.numpy as jnp
from jax import grad, jit

# обычная числовая функция
def f(x):
    return jnp.sum(jnp.sin(x) ** 2)

# производная функции — это тоже функция
df = grad(f)

# компиляция для ускорения
df_fast = jit(df)

x = jnp.array([0.0, 1.0, 2.0])
print(df_fast(x))`}
    />

    <H2>Автоматическое дифференцирование</H2>
    <P>
      <InlineCode>grad</InlineCode> возвращает функцию-градиент, которую можно
      применять повторно (например, <InlineCode>grad(grad(f))</InlineCode> для
      второй производной). Это удобно для оптимизации и реализации собственных
      алгоритмов обучения.
    </P>
    <Callout variant="info">
      JAX поддерживает как прямой, так и обратный режим дифференцирования. Для
      нейросетей чаще используют обратный режим (reverse-mode), он же — основа
      обратного распространения ошибки.
    </Callout>

    <H2>Компиляция и векторизация</H2>
    <H3>jit — ускорение</H3>
    <P>
      <InlineCode>jit</InlineCode> трассирует функцию и компилирует её в
      оптимизированный код через XLA. Первый вызов медленнее (идёт компиляция),
      зато последующие выполняются заметно быстрее.
    </P>
    <H3>vmap — батчинг</H3>
    <CodeBlock
      language="python"
      code={`from jax import vmap
import jax.numpy as jnp

def predict(w, x):
    return jnp.dot(w, x)

# применяем predict к целому батчу входов без явного цикла
batched = vmap(predict, in_axes=(None, 0))
ys = batched(weights, X_batch)`}
    />

    <H2>Особенности и экосистема</H2>
    <Ul
      items={[
        "Функциональный стиль: предпочтительны чистые функции без побочных эффектов.",
        "Явная работа со случайностью через ключи (jax.random) для воспроизводимости.",
        "Поверх JAX построены библиотеки нейросетей — Flax и Haiku, а также оптимизаторы Optax.",
      ]}
    />
    <Callout variant="tip">
      JAX дополняет, а не заменяет другие фреймворки. PyTorch и TensorFlow удобны
      для прикладной разработки, а JAX часто выбирают для исследований и
      крупных моделей, где важны скорость и масштабирование на TPU.
    </Callout>

    <H2>Когда выбирать JAX</H2>
    <Ul
      items={[
        "Нужны кастомные алгоритмы обучения и тонкий контроль над вычислениями.",
        "Важна максимальная производительность на GPU/TPU.",
        "Эксперименты требуют частого дифференцирования сложных функций.",
      ]}
    />

    <Sources refs={[REFS.goodfellow, REFS.tensorflow, REFS.nikolenko]} />
  </Article>
);

export default JaxArticle;
