import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const GptArticle: React.FC = () => (
  <Article>
    <Title>GPT</Title>
    <Lead>
      GPT — это семейство языковых моделей на основе декодировщика (decoder)
      трансформера. В отличие от BERT, GPT нацелен на порождение текста, и читает
      его слева направо, предсказывая следующее слово.
    </Lead>

    <H2>Декодер и авторегрессия</H2>
    <P>
      GPT использует декодирующую часть трансформера (Vaswani и др., 2017) с
      «маской», запрещающей модели заглядывать в будущее. Текст порождается
      авторегрессионно: модель предсказывает один токен, добавляет его к
      последовательности и предсказывает следующий — и так по цепочке.
    </P>
    <Ul
      items={[
        "Предобучение — предсказание следующего токена на огромных текстовых корпусах.",
        "Генерация — последовательное порождение токенов на основе предыдущих.",
        "Универсальность — одна модель решает множество задач без отдельной переразметки.",
      ]}
    />

    <H2>Почему это работает</H2>
    <P>
      Задача «предскажи следующий токен» вынуждает модель выучить грамматику,
      факты и связи между понятиями. На больших масштабах такие модели начинают
      решать задачи, которым их прямо не учили: переводить, суммировать,
      отвечать на вопросы и писать код.
    </P>
    <Callout variant="info">
      Растущие возможности генеративных моделей повышают и ответственность
      разработчиков: безопасное развитие таких систем закреплено в принципах
      ведущих лабораторий.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.openaiCharter, REFS.nikolenko]} />
  </Article>
);

export default GptArticle;
