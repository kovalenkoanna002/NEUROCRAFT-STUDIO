import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const FewShotArticle: React.FC = () => (
  <Article>
    <Title>Few-shot и Zero-shot обучение</Title>
    <Lead>
      Few-shot и zero-shot — это способность модели решать новую задачу, увидев
      лишь несколько примеров или вовсе ни одного. Современные большие языковые
      модели делают это прямо во время работы, без дообучения весов, просто из
      текста запроса.
    </Lead>

    <H2>Постановка проблемы</H2>
    <P>
      Классическое обучение требует тысяч размеченных примеров на каждый класс.
      Но человек узнаёт новый объект, увидев его один-два раза. Few-shot обучение
      ставит ту же цель перед моделью: обобщать по крайне малому числу примеров.
    </P>
    <Ul
      items={[
        "Zero-shot — ни одного примера задачи, только её описание словами.",
        "One-shot — один пример нужного ответа.",
        "Few-shot — несколько примеров (обычно от 2 до десятков).",
      ]}
    />

    <H2>Как это работает в языковых моделях</H2>
    <P>
      Архитектура трансформера (Vaswani и др., 2017) сделала возможным обучение в
      контексте (in-context learning): модель не меняет свои веса, а
      «подстраивается» под задачу прямо во входной последовательности. Примеры в
      запросе играют роль временной инструкции.
    </P>
    <H3>Zero-shot запрос</H3>
    <CodeBlock
      language="text"
      code={`Определи тональность отзыва (положительная/отрицательная):
Отзыв: «Доставка опоздала на три дня, упаковка помята.»
Тональность:`}
    />
    <H3>Few-shot запрос</H3>
    <CodeBlock
      language="text"
      code={`Отзыв: «Прекрасный товар, всем советую!» -> положительная
Отзыв: «Сломался через день, деньги на ветер.» -> отрицательная
Отзыв: «Доставка опоздала, упаковка помята.» ->`}
    />
    <P>
      Во втором случае модель видит формат ответа и пару образцов, поэтому
      отвечает точнее и стабильнее. При этом параметры сети остаются прежними —
      «обучение» живёт только в пределах <InlineCode>контекстного окна</InlineCode>.
    </P>

    <H2>Почему модель так умеет</H2>
    <Callout variant="info">
      На огромных корпусах текста модель встречала множество задач в формате
      «условие — ответ». Предобучение научило её распознавать такой шаблон, и при
      встрече с новым примером она по аналогии продолжает закономерность.
    </Callout>
    <P>
      Чем больше модель и чем разнообразнее её обучающие данные, тем сильнее
      проявляется few-shot способность. Это одно из эмерджентных свойств
      масштаба, замеченных у крупных языковых моделей.
    </P>

    <H2>Метрическое обучение для few-shot</H2>
    <P>
      Вне языковых моделей few-shot часто решают через метрическое обучение:
      модель учится не классифицировать, а измерять схожесть. Новый объект
      относят к тому классу, чьи примеры ему ближе всего.
    </P>
    <Ul
      items={[
        "Сиамские сети — сравнивают пары объектов и выдают меру близости.",
        "Прототипные сети — для каждого класса строят «прототип» (центр) и сравнивают с ним.",
        "Подход «обучение учиться» (meta-learning) — модель готовят так, чтобы она быстро адаптировалась к новым классам.",
      ]}
    />

    <H2>Сильные стороны и ограничения</H2>
    <Ul
      items={[
        "Плюс: мгновенное решение новых задач без сбора данных и переобучения.",
        "Плюс: один универсальный интерфейс — естественный язык.",
        "Минус: чувствительность к формулировке примеров и их порядку.",
        "Минус: точность ниже, чем у модели, дообученной на тысячах примеров.",
      ]}
    />
    <Callout variant="tip">
      Если few-shot работает нестабильно, помогает увеличить число примеров,
      сделать их разнообразнее и единообразно оформить формат ответа
      (Николенко и др.).
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default FewShotArticle;
