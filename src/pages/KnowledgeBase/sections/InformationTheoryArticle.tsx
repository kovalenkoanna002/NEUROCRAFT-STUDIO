import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const InformationTheoryArticle: React.FC = () => (
  <Article>
    <Title>Теория информации и энтропия</Title>
    <Lead>
      Теория информации даёт язык для измерения неопределённости. В глубоком
      обучении её понятия — энтропия, перекрёстная энтропия, дивергенция
      Кульбака — Лейблера — лежат в основе функций потерь для классификации.
    </Lead>

    <H2>Количество информации</H2>
    <P>
      Чем менее вероятно событие, тем больше информации оно несёт. Количество
      информации события с вероятностью <Formula>p</Formula> определяется как:
    </P>
    <Formula>I(x) = −log p(x)</Formula>
    <P>
      Логарифм по основанию 2 даёт информацию в битах, натуральный логарифм — в
      натах. Достоверное событие (<Formula>p = 1</Formula>) несёт ноль
      информации; редкое — много.
    </P>

    <H2>Энтропия</H2>
    <P>
      Энтропия — это средняя информация (мера неопределённости) случайной
      величины. Для распределения <Formula>p</Formula> она равна
      математическому ожиданию количества информации:
    </P>
    <Formula>H(p) = − Σ_x p(x) · log p(x)</Formula>
    <Ul
      items={[
        "Энтропия максимальна при равномерном распределении — когда исходы равновероятны и неопределённость наибольшая.",
        "Энтропия равна нулю, когда исход предопределён (одна вероятность равна 1).",
        "Для непрерывных величин используется дифференциальная энтропия с интегралом вместо суммы.",
      ]}
    />
    <Callout variant="info">
      Интуиция: энтропия — минимальное среднее число бит, нужное для кодирования
      сообщений из источника с данным распределением.
    </Callout>

    <H2>Перекрёстная энтропия</H2>
    <P>
      Перекрёстная энтропия измеряет «цену» кодирования данных из истинного
      распределения <Formula>p</Formula> с помощью модельного распределения{" "}
      <Formula>q</Formula>:
    </P>
    <Formula>H(p, q) = − Σ_x p(x) · log q(x)</Formula>
    <P>
      Именно она — самая распространённая <Term id="loss">функция потерь</Term>{" "}
      для классификации. Если <Formula>p</Formula> — истинная метка (one-hot), а{" "}
      <Formula>q</Formula> — выход <Term id="softmax">softmax</Term>, потеря для
      одного объекта класса <Formula>c</Formula> сводится к:
    </P>
    <Formula>L = − log q(c)</Formula>
    <P>
      Минимизация перекрёстной энтропии заставляет модель повышать предсказанную
      вероятность правильного класса.
    </P>

    <H2>Дивергенция Кульбака — Лейблера</H2>
    <P>
      KL-дивергенция измеряет, насколько распределение <Formula>q</Formula>{" "}
      отличается от <Formula>p</Formula>:
    </P>
    <Formula>D_KL(p ‖ q) = Σ_x p(x) · log( p(x) / q(x) )</Formula>
    <P>Её связь с энтропией и перекрёстной энтропией:</P>
    <Formula>H(p, q) = H(p) + D_KL(p ‖ q)</Formula>
    <H3>Свойства</H3>
    <Ul
      items={[
        "D_KL ≥ 0 всегда, и равна нулю только если p и q совпадают (неравенство Гиббса).",
        "Несимметрична: D_KL(p‖q) ≠ D_KL(q‖p), поэтому это не метрика.",
        "Поскольку H(p) от модели не зависит, минимизация перекрёстной энтропии эквивалентна минимизации KL-дивергенции.",
      ]}
    />
    <Callout variant="tip">
      KL-дивергенция используется как регуляризатор в вариационных автокодировщиках
      (VAE) и в методах дистилляции знаний, где модель-ученик приближает
      распределение модели-учителя.
    </Callout>

    <H2>Зачем это в глубоком обучении</H2>
    <Ul
      items={[
        "Перекрёстная энтропия — стандартная функция потерь для классификации (бинарной и многоклассовой).",
        "Минимизация перекрёстной энтропии эквивалентна оценке максимального правдоподобия (MLE).",
        "Энтропия выхода модели — мера уверенности: низкая энтропия означает уверенный прогноз.",
        "KL-дивергенция связывает обучение нейросетей с байесовским выводом и генеративными моделями.",
      ]}
    />
    <Callout variant="warn">
      Перекрёстная энтропия численно неустойчива, если вероятность правильного
      класса близка к нулю (log 0 = −∞). Поэтому softmax и логарифм объединяют в
      одну численно устойчивую операцию (log-softmax / cross-entropy with logits).
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default InformationTheoryArticle;
