import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const BackpropagationArticle: React.FC = () => (
  <Article>
    <Title>Обратное распространение ошибки</Title>
    <Lead>
      Backpropagation — алгоритм, который вычисляет <Term id="gradient">градиенты</Term>{" "}
      функции потерь по всем весам сети. Без него обучение глубоких сетей было бы
      невозможно. Метод популяризировали Румельхарт, Хинтон и Уильямс (1986).
    </Lead>

    <H2>Идея: правило цепочки в обратную сторону</H2>
    <P>
      Сеть — это композиция функций. Чтобы узнать, как каждый вес влияет на
      ошибку, градиент «протекает» от выхода к входу, перемножаясь по правилу
      цепочки:
    </P>
    <Formula>∂L/∂w⁽ˡ⁾ = ∂L/∂h⁽ˡ⁾ · ∂h⁽ˡ⁾/∂w⁽ˡ⁾</Formula>

    <H2>Два прохода на каждом шаге обучения</H2>
    <Ul
      items={[
        "Прямой проход: вычисляется предсказание и значение потерь.",
        "Обратный проход: от потерь к входу считаются градиенты по всем весам.",
        "Оптимизатор сдвигает веса против градиента.",
      ]}
    />

    <H2>Почему это эффективно</H2>
    <P>
      Backpropagation переиспользует промежуточные результаты, поэтому считает
      градиенты для миллионов параметров за один обратный проход. В современных
      фреймворках это делается автоматически (autograd).
    </P>

    <Callout variant="warn">
      При перемножении многих производных градиент может затухать или взрываться.
      С этим борются функцией ReLU, нормализацией и остаточными связями.
    </Callout>

    <Sources refs={[REFS.rumelhart1986, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default BackpropagationArticle;
