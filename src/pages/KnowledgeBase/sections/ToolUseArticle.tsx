import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const ToolUseArticle: React.FC = () => (
  <Article>
    <Title>Использование инструментов (tool use)</Title>
    <Lead>
      Использование инструментов — это способность языковой модели не только
      генерировать текст, но и вызывать внешние функции и сервисы: поиск,
      калькулятор, базы данных, API. Так модель выходит за пределы того, что
      «помнит».
    </Lead>

    <H2>Как работает вызов функций</H2>
    <P>
      При функциональном вызове (function calling) модели заранее описывают
      доступные функции: их имена, назначение и параметры. Когда для ответа нужен
      инструмент, модель возвращает не обычный текст, а структурированный вызов —
      имя функции и аргументы. Программа выполняет вызов и передаёт результат
      обратно модели, которая продолжает рассуждение.
    </P>
    <Ul
      items={[
        "Модель решает, какой инструмент нужен и с какими параметрами его вызвать.",
        "Внешний код исполняет вызов и возвращает результат.",
        "Модель использует результат, чтобы сформировать окончательный ответ.",
      ]}
    />

    <H2>Зачем это нужно</H2>
    <P>
      Языковая модель не знает свежих данных, может ошибаться в арифметике и не
      имеет прямого доступа к вашим системам. Инструменты решают эти проблемы:
      дают актуальную информацию, точные вычисления и возможность совершать
      действия. Именно tool use превращает модель из «собеседника» в основу для
      ИИ-агентов.
    </P>
    <Callout variant="tip">
      Доступ модели к внешним действиям требует продуманных ограничений — вопросы
      безопасного развёртывания таких возможностей закреплены в принципах ведущих
      лабораторий.
    </Callout>

    <Sources refs={[REFS.openaiCharter, REFS.nikolenko]} />
  </Article>
);

export default ToolUseArticle;
