import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ComputationalGraphArticle: React.FC = () => (
  <Article>
    <Title>Вычислительный граф</Title>
    <Lead>
      Вычислительный граф — это представление вычислений в виде сети узлов и
      рёбер. На нём фреймворки глубокого обучения организуют прямой проход и
      автоматически вычисляют градиенты через обратное распространение.
    </Lead>

    <H2>Что это такое</H2>
    <P>
      Вычислительный граф — ориентированный граф без циклов (DAG), где узлы — это
      операции или переменные, а рёбра показывают поток данных. Любое выражение
      разбивается на элементарные операции. Например, <Formula>L = (w·x + b − y)²</Formula>{" "}
      раскладывается в последовательность шагов:
    </P>
    <Ul
      items={[
        "u = w · x — умножение",
        "v = u + b — сложение",
        "d = v − y — вычитание",
        "L = d² — возведение в квадрат",
      ]}
    />
    <Callout variant="info">
      Каждый узел знает, как вычислить свой выход по входам (прямой проход) и как
      вычислить производную выхода по входам (для обратного прохода).
    </Callout>

    <H2>Прямой проход</H2>
    <P>
      Прямой проход (forward pass) — вычисление значения функции от входов к
      выходу в порядке топологической сортировки графа. Каждый узел берёт
      результаты родителей и применяет свою операцию. Промежуточные значения
      сохраняются — они понадобятся при обратном проходе.
    </P>
    <Formula>x → (·w) → (+b) → (−y) → (²) → L</Formula>

    <H2>Обратный проход и правило цепочки</H2>
    <P>
      Чтобы обучать сеть, нужны производные <Term id="loss">потерь</Term> по
      параметрам. Они вычисляются обратным проходом (backward pass): градиент
      «протекает» от выхода ко входам, перемножаясь по правилу цепочки.
    </P>
    <Formula>∂L/∂w = (∂L/∂v) · (∂v/∂u) · (∂u/∂w)</Formula>
    <P>
      Каждый узел получает градиент потерь по своему выходу и передаёт градиент по
      своим входам родителям. Это и есть <Term id="backprop">обратное
      распространение</Term>.
    </P>
    <H3>Ключевое преимущество</H3>
    <Ul
      items={[
        "Промежуточные значения прямого прохода переиспользуются — не нужно пересчитывать всё заново.",
        "Стоимость вычисления всех градиентов сопоставима со стоимостью одного прямого прохода.",
        "Граф позволяет автоматически дифференцировать произвольно сложные выражения.",
      ]}
    />

    <H2>Статические и динамические графы</H2>
    <P>
      Фреймворки строят граф по-разному:
    </P>
    <Ul
      items={[
        "Статический граф (define-and-run): сначала описывается весь граф, потом запускается. Так работал ранний TensorFlow 1.x — это даёт возможности оптимизации, но усложняет отладку.",
        "Динамический граф (define-by-run): граф строится «на лету» во время выполнения кода. Так работает PyTorch — это удобнее для отладки и условной логики.",
      ]}
    />
    <Callout variant="tip">
      Динамические графы позволяют использовать обычные циклы и условия Python:
      граф каждый раз пересобирается под фактический путь выполнения.
    </Callout>

    <H2>Граф в PyTorch на практике</H2>
    <P>
      В PyTorch граф строится автоматически по мере выполнения операций над
      тензорами с <InlineCode>requires_grad=True</InlineCode>. Вызов{" "}
      <InlineCode>backward()</InlineCode> запускает обратный проход.
    </P>
    <CodeBlock
      language="python"
      code={`import torch

w = torch.tensor(2.0, requires_grad=True)
x = torch.tensor(3.0)
b = torch.tensor(1.0, requires_grad=True)
y = torch.tensor(5.0)

L = (w * x + b - y) ** 2   # строится граф вычислений
L.backward()               # обратный проход по графу

print(w.grad)   # ∂L/∂w
print(b.grad)   # ∂L/∂b`}
    />

    <H2>Зачем это понимать</H2>
    <Ul
      items={[
        "Вычислительный граф — основа автоматического дифференцирования (autograd).",
        "Понимание графа объясняет, почему промежуточные активации хранятся в памяти до backward (отсюда расход памяти GPU).",
        "Объясняет проблему исчезающего градиента: при длинном пути произведение производных может стремиться к нулю.",
        "Помогает осознанно использовать приёмы вроде detach, no_grad и gradient checkpointing.",
      ]}
    />
    <Callout variant="warn">
      Все промежуточные значения хранятся до обратного прохода — поэтому глубокие
      сети с большими батчами требуют много памяти. Приём gradient checkpointing
      экономит память, пересчитывая часть значений заново.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.pytorch]} />
  </Article>
);

export default ComputationalGraphArticle;
