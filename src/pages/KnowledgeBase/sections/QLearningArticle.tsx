import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const QLearningArticle: React.FC = () => (
  <Article>
    <Title>Q-обучение</Title>
    <Lead>
      Q-обучение (Q-learning) — один из базовых алгоритмов обучения с
      подкреплением. Агент учится оценивать «качество» (отсюда Q, от англ.
      quality) каждого действия в каждом состоянии и постепенно выбирает те
      действия, которые ведут к максимальной суммарной награде. Метод предложил
      Кристофер Уоткинс в 1989 году; доказательство сходимости опубликовано в
      1992-м.
    </Lead>

    <H2>Постановка задачи</H2>
    <P>
      Среда описывается марковским процессом принятия решений (MDP): множеством
      состояний <Formula>S</Formula>, множеством действий <Formula>A</Formula>,
      функцией перехода и функцией награды. На каждом шаге агент в состоянии{" "}
      <Formula>s</Formula> выбирает действие <Formula>a</Formula>, получает
      награду <Formula>r</Formula> и попадает в новое состояние{" "}
      <Formula>s'</Formula>. Цель — найти политику, максимизирующую ожидаемую
      дисконтированную сумму наград.
    </P>
    <Ul
      items={[
        "Состояние — описание текущей ситуации в среде.",
        "Действие — что агент может сделать в этом состоянии.",
        "Награда — числовой сигнал обратной связи за шаг.",
        "Коэффициент дисконтирования γ ∈ [0, 1] — насколько важна будущая награда по сравнению с немедленной.",
      ]}
    />

    <H2>Функция ценности действия</H2>
    <P>
      Центральный объект — функция <Formula>Q(s, a)</Formula>, которая оценивает
      ожидаемую суммарную награду, если из состояния <Formula>s</Formula>{" "}
      выполнить действие <Formula>a</Formula>, а дальше действовать оптимально.
      Оптимальная функция удовлетворяет уравнению Беллмана:
    </P>
    <P>
      <Formula>
        Q*(s, a) = E[ r + γ · max&#x2090; Q*(s', a') ]
      </Formula>
    </P>
    <P>
      Зная <Formula>Q*</Formula>, оптимальную политику получают жадно: в каждом
      состоянии выбирают действие с наибольшим значением{" "}
      <Formula>Q*(s, a)</Formula>.
    </P>

    <H2>Правило обновления</H2>
    <P>
      Q-обучение приближает <Formula>Q*</Formula> итеративно. После каждого
      перехода <Formula>(s, a, r, s')</Formula> значение в таблице корректируется:
    </P>
    <P>
      <Formula>
        Q(s, a) ← Q(s, a) + α · [ r + γ · max&#x2090; Q(s', a') − Q(s, a) ]
      </Formula>
    </P>
    <Ul
      items={[
        "α — скорость обучения (learning rate): размер шага коррекции.",
        "Выражение в скобках — ошибка временной разности (TD-ошибка).",
        "r + γ · max Q(s', a') — целевое значение (target), к которому подтягивается оценка.",
      ]}
    />
    <Callout variant="info">
      Q-обучение относится к off-policy методам: целевое значение использует
      жадное действие <InlineCode>max</InlineCode>, даже если в реальности агент
      во время обучения выбрал другое (например, исследовательское) действие.
    </Callout>

    <H2>Исследование и использование</H2>
    <P>
      Если всегда выбирать действие с максимальным <Formula>Q</Formula>, агент
      рискует застрять в неоптимальной стратегии и не изучить среду. Поэтому
      применяют политику ε-жадного выбора (ε-greedy): с вероятностью{" "}
      <Formula>ε</Formula> агент берёт случайное действие, иначе — лучшее по
      текущей оценке. Со временем <Formula>ε</Formula> обычно уменьшают.
    </P>

    <H2>Псевдокод табличного Q-обучения</H2>
    <CodeBlock
      language="python"
      code={`import numpy as np

# Q — таблица оценок: строки — состояния, столбцы — действия
Q = np.zeros((n_states, n_actions))
alpha, gamma, eps = 0.1, 0.99, 0.1

for episode in range(n_episodes):
    s = env.reset()
    done = False
    while not done:
        # ε-жадный выбор действия
        if np.random.rand() < eps:
            a = np.random.randint(n_actions)
        else:
            a = int(np.argmax(Q[s]))

        s_next, r, done = env.step(a)

        # обновление по правилу временной разности
        target = r + gamma * np.max(Q[s_next])
        Q[s, a] += alpha * (target - Q[s, a])

        s = s_next`}
    />

    <H2>Возможности и ограничения</H2>
    <H3>Сильные стороны</H3>
    <Ul
      items={[
        "Простота: не требует модели среды (model-free).",
        "Доказана сходимость к Q* при достаточном исследовании и убывающем α.",
        "Хорошо работает на задачах с небольшим дискретным числом состояний и действий.",
      ]}
    />
    <H3>Ограничения</H3>
    <Ul
      items={[
        "Таблица Q растёт как число состояний × число действий — для больших пространств это невозможно хранить.",
        "Не подходит напрямую для непрерывных состояний (изображения, координаты).",
        "Медленная сходимость при разреженных наградах.",
      ]}
    />
    <Callout variant="tip">
      Чтобы преодолеть ограничения таблицы, функцию <Formula>Q</Formula>{" "}
      приближают нейросетью — так появляется Deep Q-Network (DQN), масштабирующий
      идею Q-обучения на сложные среды.
    </Callout>

    <Sources refs={[REFS.watkins1992, REFS.sutton2018, REFS.nikolenko]} />
  </Article>
);

export default QLearningArticle;
