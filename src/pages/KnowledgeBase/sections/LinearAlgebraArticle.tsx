import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const LinearAlgebraArticle: React.FC = () => (
  <Article>
    <Title>Линейная алгебра</Title>
    <Lead>
      Линейная алгебра — язык нейронных сетей. Данные представляются векторами и
      матрицами, а работа слоя — это матричное умножение.
    </Lead>

    <H2>Базовые объекты</H2>
    <Ul
      items={[
        "Скаляр — одно число.",
        "Вектор — упорядоченный набор чисел (например, признаки одного объекта).",
        "Матрица — прямоугольная таблица чисел (например, веса слоя).",
        "Тензор — обобщение на любое число измерений (например, батч RGB-изображений 4D).",
      ]}
    />

    <H2>Главная операция — умножение матрицы на вектор</H2>
    <P>
      Полносвязный слой вычисляет именно её: вход x умножается на матрицу весов
      W, добавляется смещение b:
    </P>
    <Formula>h = W · x + b</Formula>
    <P>
      Если у слоя n входов и m нейронов, то W имеет размер m × n. Каждая строка W
      — это веса одного нейрона.
    </P>

    <H2>Что важно помнить</H2>
    <Ul
      items={[
        "Размерности должны согласовываться: (m×n)·(n×1) = (m×1).",
        "Скалярное произведение измеряет «похожесть» векторов — основа механизма внимания.",
        "Операции над матрицами эффективно распараллеливаются на GPU.",
      ]}
    />

    <Callout variant="tip">
      Почти любое вычисление в сети сводится к матричным умножениям и поэлементным
      нелинейностям — поэтому фреймворки оптимизируют именно их.
    </Callout>

    <H2>Скалярное произведение и «похожесть»</H2>
    <P>
      Скалярное произведение двух векторов тем больше, чем сильнее они «смотрят»
      в одну сторону. На этом основан механизм внимания в трансформерах:
      похожесть запроса и ключа измеряется именно так.
    </P>
    <Formula>a · b = a₁b₁ + a₂b₂ + … + aₙbₙ</Formula>

    <H2>Батчи как матрицы</H2>
    <P>
      На практике в сеть подают сразу пакет (батч) объектов. Тогда вход — это
      матрица X размера (batch × признаки), а слой считает X·Wᵀ + b одной
      операцией для всего пакета. Это и делает обучение на GPU эффективным.
    </P>

    <Callout variant="info">
      Понятия нормы вектора (длины) и единичных векторов нужны для нормализации
      признаков и интерпретации эмбеддингов.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.kubsuProgram]} />
  </Article>
);

export default LinearAlgebraArticle;
