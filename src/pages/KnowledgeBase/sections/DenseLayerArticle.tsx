import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  CodeBlock,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const DenseLayerArticle: React.FC = () => (
  <Article>
    <Title>Полносвязный слой</Title>
    <Lead>
      Полносвязный (dense, fully connected) слой — базовый строительный блок
      нейросетей. В нём каждый нейрон соединён со всеми входами предыдущего слоя.
      Это линейное преобразование с обучаемыми весами, за которым следует
      нелинейность.
    </Lead>

    <H2>Устройство слоя</H2>
    <P>
      <Term id="dense">Полносвязный слой</Term> принимает вектор входов{" "}
      <Formula>x</Formula> размерности <Formula>n</Formula> и выдаёт вектор{" "}
      <Formula>y</Formula> размерности <Formula>m</Formula>. Каждый из{" "}
      <Formula>m</Formula> <Term id="neuron">нейронов</Term> вычисляет
      взвешенную сумму всех входов плюс смещение, после чего применяется{" "}
      <Term id="activation">функция активации</Term>.
    </P>
    <Formula>y = σ( W·x + b )</Formula>
    <Ul
      items={[
        "W — матрица весов размера m×n (каждая строка — веса одного нейрона).",
        "b — вектор смещений размера m.",
        "σ — функция активации, применяемая поэлементно.",
        "Всего обучаемых параметров: m·n + m.",
      ]}
    />
    <Callout variant="info">
      «Полносвязный» означает, что между входным и выходным слоями нет
      пропущенных связей: матрица W плотная, без нулей по структуре.
    </Callout>

    <H2>Прямой проход одного нейрона</H2>
    <P>
      Каждый нейрон сначала считает линейную комбинацию (предактивацию){" "}
      <Formula>z</Formula>, затем пропускает её через активацию:
    </P>
    <Formula>zᵢ = Σⱼ Wᵢⱼ·xⱼ + bᵢ</Formula>
    <Formula>aᵢ = σ(zᵢ)</Formula>
    <P>
      <Term id="weight">Веса</Term> задают, насколько важен каждый вход;{" "}
      <Term id="bias">смещение</Term> сдвигает порог срабатывания.
    </P>

    <H2>Работа с батчами</H2>
    <P>
      На практике слой обрабатывает не один пример, а батч из{" "}
      <Formula>N</Formula> примеров одновременно. Тогда вход — матрица{" "}
      <Formula>X</Formula> размера <Formula>N×n</Formula>, и вычисления
      записываются одним матричным умножением:
    </P>
    <Formula>Y = σ( X·Wᵀ + b )</Formula>
    <CodeBlock
      language="python"
      code={`import torch.nn as nn

layer = nn.Linear(in_features=64, out_features=10)
# веса: (10, 64), смещения: (10,)

import torch
x = torch.randn(32, 64)   # батч из 32 примеров
y = layer(x)              # линейное преобразование
print(y.shape)            # torch.Size([32, 10])`}
    />

    <H2>Зачем нужна нелинейность</H2>
    <P>
      Если убрать активацию, цепочка полносвязных слоёв схлопывается в одно
      линейное преобразование: композиция линейных функций линейна. Нелинейная
      активация (<Term id="relu">ReLU</Term>, <Term id="sigmoid">сигмоида</Term>,{" "}
      <Term id="tanh">tanh</Term>) позволяет сети выражать сложные зависимости.
    </P>
    <H3>Типичные активации</H3>
    <Ul
      items={[
        "ReLU — стандарт для скрытых слоёв: быстрая, не насыщается для положительных значений.",
        "Sigmoid — для бинарной классификации на выходе (вероятность).",
        "Softmax — для многоклассовой классификации на выходе (распределение).",
        "Tanh — симметричная относительно нуля, иногда удобна в рекуррентных сетях.",
      ]}
    />
    <Callout variant="tip">
      Без нелинейности глубокая сеть из dense-слоёв эквивалентна одному слою —
      именно активации делают глубину осмысленной.
    </Callout>

    <H2>Роль в архитектурах</H2>
    <Ul
      items={[
        "Скрытые слои многослойного перцептрона (MLP) — это стек полносвязных слоёв.",
        "В свёрточных сетях dense-слои обычно идут в конце — как классификатор поверх извлечённых признаков.",
        "Перед полносвязным слоем карты признаков разворачивают в вектор операцией flatten.",
        "В трансформерах внутри каждого блока есть полносвязная feed-forward подсеть.",
      ]}
    />

    <H2>Сильные и слабые стороны</H2>
    <Ul
      items={[
        "Плюс: универсальность — может приближать произвольные зависимости (теорема об аппроксимации).",
        "Плюс: простота и эффективность вычислений (одно матричное умножение).",
        "Минус: много параметров — m·n весов растёт быстро при больших входах.",
        "Минус: не учитывает структуру данных (для изображений эффективнее свёртки с разделяемыми весами).",
      ]}
    />
    <Callout variant="warn">
      Для изображений «в лоб» полносвязный слой неэффективен: картинка 224×224×3
      даёт более 150 тысяч входов на нейрон. Поэтому используют свёртки, которые
      разделяют веса.
    </Callout>

    <Sources refs={[REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default DenseLayerArticle;
