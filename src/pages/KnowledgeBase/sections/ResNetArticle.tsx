import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const ResNetArticle: React.FC = () => (
  <Article>
    <Title>Остаточные сети (ResNet)</Title>
    <Lead>
      ResNet (He и др., 2016) решил проблему обучения очень глубоких сетей с
      помощью остаточных (skip) связей. Это позволило обучать сети из сотен слоёв
      и стало стандартом в компьютерном зрении.
    </Lead>

    <H2>Проблема деградации</H2>
    <P>
      Парадоксально, но простое наращивание слоёв ухудшало качество — даже на
      обучении. Дело не в переобучении, а в трудности оптимизации очень глубоких
      сетей.
    </P>

    <H2>Остаточная связь</H2>
    <P>
      Вместо того чтобы слой выучивал отображение H(x), он учит «остаток» F(x), а
      вход добавляется напрямую:
    </P>
    <Formula>y = F(x) + x</Formula>
    <P>
      Если оптимально ничего не менять, сети достаточно занулить F(x) — а это
      легко. Skip-связи также дают градиенту «короткий путь» назад, ослабляя
      затухание.
    </P>

    <H2>Значение</H2>
    <Ul
      items={[
        "Сделали практичными сети из 50, 101, 152 слоёв.",
        "Остаточные связи теперь используют почти везде, включая трансформеры.",
        "ResNet — частая основа для transfer learning.",
      ]}
    />

    <Sources refs={[REFS.he2016, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default ResNetArticle;
