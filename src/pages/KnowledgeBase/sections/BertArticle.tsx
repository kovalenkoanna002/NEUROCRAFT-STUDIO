import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const BertArticle: React.FC = () => (
  <Article>
    <Title>BERT</Title>
    <Lead>
      BERT — это языковая модель на основе кодировщика (encoder) трансформера,
      предназначенная для задач понимания текста. Её ключевая особенность —
      двунаправленность: каждое слово рассматривается одновременно по левому и
      правому контексту.
    </Lead>

    <H2>Архитектура и двунаправленность</H2>
    <P>
      BERT использует только кодирующую часть трансформера (Vaswani и др., 2017).
      В отличие от моделей, читающих текст слева направо, BERT видит всё
      предложение целиком. Благодаря этому представление каждого слова учитывает и
      то, что было до него, и то, что после, — это и называется
      двунаправленностью.
    </P>

    <H2>Masked language modeling и дообучение</H2>
    <P>
      Предобучается BERT методом маскированного языкового моделирования (masked
      language modeling): часть слов в тексте случайно заменяют на специальный
      токен, и модель учится восстанавливать их по контексту. Так разметка не
      нужна — метки берутся из самого текста.
    </P>
    <Ul
      items={[
        "Классификация текста — определение тональности, темы, спама.",
        "Ответы на вопросы — поиск фрагмента-ответа в тексте.",
        "Распознавание именованных сущностей и другие задачи понимания.",
      ]}
    />
    <Callout variant="tip">
      После предобучения BERT дообучают под конкретную задачу на небольшом
      размеченном наборе — добавляют простой слой поверх и обновляют веса. Это
      пример переноса обучения в обработке текста.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default BertArticle;
