import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const PpoArticle: React.FC = () => (
  <Article>
    <Title>Proximal Policy Optimization (PPO)</Title>
    <Lead>
      PPO (Schulman и др., 2017) — один из самых популярных алгоритмов обучения с
      подкреплением. Это метод актёр-критик, который ограничивает размер шага
      обновления политики, оставаясь при этом простым в реализации. PPO стал
      стандартным выбором во многих задачах, включая дообучение языковых моделей
      по обратной связи людей (RLHF).
    </Lead>

    <H2>Зачем ограничивать шаг</H2>
    <P>
      В методах градиента политики слишком большой шаг обновления может резко
      ухудшить политику, и восстановиться после этого трудно. Предшественник PPO
      — TRPO — решал это через сложное ограничение на KL-дивергенцию между старой
      и новой политикой. PPO достигает похожего эффекта проще и дешевле.
    </P>
    <Ul
      items={[
        "Цель — менять политику ощутимо, но не «прыгать» слишком далеко за один апдейт.",
        "Это повышает стабильность и устойчивость к выбору гиперпараметров.",
        "Можно делать несколько эпох обучения на одном собранном батче данных.",
      ]}
    />

    <H2>Отношение вероятностей</H2>
    <P>
      Ключевая величина — отношение вероятностей новой и старой политики для
      одного и того же действия:
    </P>
    <P>
      <Formula>
        r&#x209C;(θ) = π(a&#x209C; | s&#x209C;; θ) / π(a&#x209C; | s&#x209C;;
        θ_old)
      </Formula>
    </P>
    <P>
      Если <Formula>r&#x209C; &gt; 1</Formula>, новая политика повышает
      вероятность действия; если <Formula>r&#x209C; &lt; 1</Formula> — понижает.
    </P>

    <H2>Обрезанная целевая функция</H2>
    <P>
      Главная идея PPO — «обрезка» (clipping): отношение{" "}
      <Formula>r&#x209C;</Formula> зажимается в коридоре{" "}
      <Formula>[1 − ε, 1 + ε]</Formula>, чтобы убрать стимул к чрезмерному
      изменению политики:
    </P>
    <P>
      <Formula>
        L = E[ min( r&#x209C; · A&#x209C;, clip(r&#x209C;, 1−ε, 1+ε) · A&#x209C;
        ) ]
      </Formula>
    </P>
    <Ul
      items={[
        "Aₜ — оценка преимущества (advantage), обычно через GAE.",
        "ε — порог обрезки, типично 0.1–0.2.",
        "Берётся минимум обрезанной и необрезанной версии — это даёт нижнюю (пессимистичную) границу улучшения.",
      ]}
    />
    <Callout variant="info">
      Полная функция потерь PPO складывается из трёх частей: обрезанная цель
      актёра, ошибка критика (оценка ценности) и бонус за энтропию для поощрения
      исследования.
    </Callout>

    <H2>Расчёт обрезанной цели</H2>
    <CodeBlock
      language="python"
      code={`import torch

def ppo_loss(logp_new, logp_old, advantage, eps=0.2):
    # отношение вероятностей (через экспоненту разности логарифмов)
    ratio = torch.exp(logp_new - logp_old)

    # необрезанная и обрезанная цели
    unclipped = ratio * advantage
    clipped = torch.clamp(ratio, 1 - eps, 1 + eps) * advantage

    # берём пессимистичный минимум, минус — для минимизации
    actor_loss = -torch.min(unclipped, clipped).mean()
    return actor_loss`}
    />

    <H2>Схема обучения</H2>
    <Ul
      items={[
        "Собрать батч траекторий текущей политикой (on-policy).",
        "Оценить преимущества Aₜ и целевые значения для критика.",
        "Несколько эпох оптимизировать обрезанную цель на мини-батчах.",
        "Обновить старую политику и повторить.",
      ]}
    />

    <H2>Почему PPO так популярен</H2>
    <H3>Плюсы</H3>
    <Ul
      items={[
        "Простая реализация по сравнению с TRPO при сопоставимом качестве.",
        "Устойчивость к гиперпараметрам и широкому классу задач.",
        "Эффективное переиспользование батча за несколько эпох.",
      ]}
    />
    <H3>Минусы</H3>
    <Ul
      items={[
        "Остаётся on-policy: данные собираются заново после каждого крупного обновления.",
        "Требует аккуратной нормировки наград и настройки коэффициентов потерь.",
      ]}
    />
    <Callout variant="tip">
      В RLHF PPO дообучает языковую модель (актёра) так, чтобы повышать награду
      от модели вознаграждения, при этом <InlineCode>clip</InlineCode> и штраф за
      KL удерживают её близко к исходной модели и не дают «сломаться».
    </Callout>

    <Sources refs={[REFS.schulman2017, REFS.sutton2018]} />
  </Article>
);

export default PpoArticle;
