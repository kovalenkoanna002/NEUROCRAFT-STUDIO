import React from "react";
import { Article, Title, Lead, H2, P, Ul, Callout, Sources } from "../components/ArticleKit";
import { REFS } from "../references";

const LoraArticle: React.FC = () => (
  <Article>
    <Title>Дообучение и LoRA</Title>
    <Lead>
      Дообучить большую модель можно по-разному. Полное дообучение меняет все веса
      и стоит дорого, а параметро-эффективные методы вроде LoRA настраивают лишь
      малую часть параметров, сохраняя почти то же качество.
    </Lead>

    <H2>Полное дообучение</H2>
    <P>
      При полном дообучении (full fine-tuning) обновляются все параметры модели.
      Это гибко, но затратно: нужно хранить и пересчитывать миллиарды весов, а для
      каждой новой задачи приходится держать отдельную полную копию модели.
    </P>
    <Ul
      items={[
        "Высокие требования к видеопамяти и вычислениям.",
        "Отдельная полная копия весов под каждую задачу.",
        "Риск переобучения на небольшом наборе данных.",
      ]}
    />

    <H2>Параметро-эффективное дообучение</H2>
    <P>
      Параметро-эффективные методы замораживают исходные веса и обучают лишь
      небольшой набор дополнительных параметров. Базовая модель остаётся общей, а
      под задачу хранится только маленькая «надстройка», что резко экономит память.
    </P>

    <H2>Идея низкоранговых адаптеров (LoRA)</H2>
    <P>
      LoRA (Low-Rank Adaptation) исходит из того, что изменение весов при
      дообучении можно приблизить матрицей низкого ранга. Большую матрицу поправки
      раскладывают в произведение двух маленьких матриц A и B, и обучают только их.
      Исходные веса не трогают.
    </P>
    <Callout variant="tip">
      Адаптеры LoRA весят в сотни раз меньше полной модели, поэтому под каждую
      задачу можно хранить свой компактный набор и быстро переключаться между ними.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.nikolenko]} />
  </Article>
);

export default LoraArticle;
