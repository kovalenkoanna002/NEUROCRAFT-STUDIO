import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  CodeBlock,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const VectorDbArticle: React.FC = () => (
  <Article>
    <Title>Векторные базы данных</Title>
    <Lead>
      Векторная база данных хранит объекты не как строки таблицы, а как
      числовые векторы (эмбеддинги) и умеет быстро находить ближайшие к заданному
      запросу. Это фундамент семантического поиска и архитектуры
      Retrieval-Augmented Generation (RAG), на которой строятся современные
      ассистенты, отвечающие по корпоративным документам.
    </Lead>

    <H2>Зачем нужны векторные базы</H2>
    <P>
      Классическая база данных ищет по точному совпадению или по ключевым
      словам: запрос «договор аренды» найдёт только документы, где эти слова
      встречаются буквально. Но смысл текста можно выразить плотным вектором —
      эмбеддингом, — и тогда «съёмная квартира» и «аренда жилья» окажутся рядом в
      векторном пространстве, даже если ни одного общего слова в них нет. Идея
      семантической близости через арифметику над векторами слов восходит к
      Word2Vec (Mikolov и др., 2013).
    </P>
    <Ul
      items={[
        "Семантический поиск: ищем по смыслу, а не по совпадению слов.",
        "Рекомендации: находим похожие товары, статьи, пользователей.",
        "Дедупликация и кластеризация близких объектов.",
        "Память для языковых моделей: подкладываем релевантные фрагменты в контекст.",
      ]}
    />

    <H2>Эмбеддинги: что хранится внутри</H2>
    <P>
      Каждый объект — фрагмент текста, изображение, аудио — прогоняется через
      модель-кодировщик и превращается в вектор фиксированной длины (например,
      384, 768 или 1536 чисел). Близость двух векторов чаще всего измеряют
      косинусным сходством:
    </P>
    <Formula>cos(a, b) = (a · b) / (‖a‖ · ‖b‖)</Formula>
    <P>
      Значение, близкое к 1, означает почти одинаковое направление векторов и,
      следовательно, близкий смысл. Иногда вместо косинуса используют евклидово
      расстояние или скалярное произведение — выбор метрики должен совпадать с
      тем, на что обучался кодировщик.
    </P>
    <Callout variant="info">
      Качество поиска определяется в первую очередь кодировщиком эмбеддингов, а
      не самой базой. База лишь быстро находит ближайших соседей среди уже
      посчитанных векторов.
    </Callout>

    <H2>Поиск ближайших соседей и индексы ANN</H2>
    <P>
      Наивный поиск перебирает все векторы и считает расстояние до каждого — это
      точно, но медленно при миллионах записей. Поэтому используют приближённый
      поиск ближайших соседей (Approximate Nearest Neighbors, ANN): он жертвует
      малой долей точности ради огромного ускорения.
    </P>
    <H3>Популярные индексы</H3>
    <Ul
      items={[
        "HNSW — многоуровневый граф «маленького мира»: быстрый поиск и высокая полнота, но требует памяти.",
        "IVF — разбиение пространства на кластеры; запрос проверяет только ближайшие кластеры.",
        "PQ (Product Quantization) — сжатие векторов для экономии памяти ценой точности.",
        "Часто комбинируют, например IVF+PQ, чтобы балансировать скорость, память и точность.",
      ]}
    />
    <Callout variant="tip">
      Ключевой компромисс — между полнотой (recall) и скоростью. Параметры
      индекса настраивают так, чтобы потеря релевантных результатов оставалась
      приемлемой для задачи.
    </Callout>

    <H2>RAG: векторная база как память модели</H2>
    <P>
      Подход Retrieval-Augmented Generation (Lewis и др., 2020) соединяет поиск и
      генерацию: вместо того чтобы полагаться только на «память» языковой модели,
      система сначала находит релевантные фрагменты во внешней базе знаний, а
      затем передаёт их модели как контекст. Это снижает выдумывание фактов и
      позволяет отвечать по свежим или приватным данным без переобучения модели.
    </P>
    <Ul
      items={[
        "Документы режут на фрагменты (чанки) и кодируют в векторы — этап индексации.",
        "Запрос пользователя тоже кодируется в вектор.",
        "Векторная база возвращает top-k ближайших фрагментов.",
        "Фрагменты подставляются в промпт, и модель формулирует ответ со ссылкой на источник.",
      ]}
    />

    <H2>Практический пример</H2>
    <P>
      Минимальный сценарий: посчитать эмбеддинги, сложить их в индекс и найти
      ближайших соседей. Здесь показана идея на уровне псевдо-API; в реальных
      проектах используют готовые библиотеки и СУБД.
    </P>
    <CodeBlock
      language="python"
      code={`# Идея: индексируем тексты и ищем по смыслу
vectors = encoder.encode(documents)   # [N, d] эмбеддинги
index.add(vectors)                     # строим ANN-индекс (например, HNSW)

q = encoder.encode(["условия аренды жилья"])
ids, dist = index.search(q, k=5)       # top-5 ближайших фрагментов

context = [documents[i] for i in ids[0]]
answer = llm.generate(query, context)  # RAG: генерация с опорой на контекст`}
    />
    <Callout variant="warn">
      Векторный поиск не понимает смысла «сам по себе»: если кодировщик обучен на
      другой предметной области или языке, близость векторов перестаёт отражать
      смысл. Для русскоязычных задач выбирайте кодировщик, обученный на русском.
    </Callout>

    <H2>Что важно помнить</H2>
    <Ul
      items={[
        <>
          Векторная база — это быстрый поиск ближайших соседей по{" "}
          <Term id="weight">векторам</Term>, а не «умная» система сама по себе.
        </>,
        "Метрика близости должна совпадать с той, на которой обучался кодировщик.",
        "ANN-индексы дают скорость ценой небольшой потери полноты.",
        "В связке RAG векторная база служит внешней памятью языковой модели.",
      ]}
    />

    <Sources refs={[REFS.lewis2020, REFS.mikolov2013, REFS.nikolenko]} />
  </Article>
);

export default VectorDbArticle;
