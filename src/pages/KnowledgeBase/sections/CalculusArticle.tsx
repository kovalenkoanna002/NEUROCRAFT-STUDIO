import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const CalculusArticle: React.FC = () => (
  <Article>
    <Title>Математический анализ</Title>
    <Lead>
      Обучение сети — это оптимизация функции потерь. Чтобы понять, как менять
      веса, нужны производные, частные производные и правило цепочки.
    </Lead>

    <H2>Производная и градиент</H2>
    <P>
      Производная показывает, как быстро меняется функция. Для функции многих
      переменных это <Term id="gradient">градиент</Term> — вектор частных
      производных по каждому параметру. Он указывает направление наискорейшего
      роста функции:
    </P>
    <Formula>∇L = ( ∂L/∂w₁, ∂L/∂w₂, …, ∂L/∂wₙ )</Formula>

    <H2>Правило цепочки</H2>
    <P>
      Сеть — это композиция функций (слой за слоем). Производная композиции
      считается по правилу цепочки:
    </P>
    <Formula>d/dx f(g(x)) = f′(g(x)) · g′(x)</Formula>
    <P>
      Именно на этом основан алгоритм{" "}
      <Term id="backprop">обратного распространения</Term>: градиент потерь
      «протекает» от выхода к входу, перемножаясь по слоям.
    </P>

    <H2>Зачем это на практике</H2>
    <Ul
      items={[
        "Градиент даёт направление, в котором надо сдвинуть веса, чтобы уменьшить ошибку.",
        "Если производная активации близка к нулю, градиент «затухает» — отсюда проблема исчезающего градиента в глубоких сетях.",
        "Фреймворки (TensorFlow, PyTorch) считают производные автоматически (autograd).",
      ]}
    />

    <Callout variant="tip">
      Понимание цепного правила объясняет, почему выбор функции активации влияет
      на обучаемость глубокой сети.
    </Callout>

    <H2>Автоматическое дифференцирование</H2>
    <P>
      Вручную выводить производные для миллионов параметров невозможно. Фреймворки
      строят граф вычислений и применяют правило цепочки автоматически (autograd):
      достаточно описать прямой проход, а градиенты считаются сами.
    </P>

    <H2>Исчезающий и взрывающийся градиент</H2>
    <Ul
      items={[
        "Если производные по модулю < 1, при перемножении по многим слоям градиент стремится к нулю — ранние слои не обучаются.",
        "Если > 1 — градиент растёт лавинообразно, обучение разрушается.",
        "Средства борьбы: ReLU, нормализация, остаточные связи, ограничение нормы градиента (clipping).",
      ]}
    />

    <Sources refs={[REFS.rumelhart1986, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default CalculusArticle;
