import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  P,
  Ul,
  Formula,
  Callout,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const TransformersArticle: React.FC = () => (
  <Article>
    <Title>Трансформеры (Transformers)</Title>
    <Lead>
      Трансформер (Vaswani и др., 2017) отказался от рекуррентности в пользу
      механизма внимания. Это основа современных языковых моделей (GPT, BERT) и
      многих моделей зрения.
    </Lead>

    <H2>Механизм внимания (attention)</H2>
    <P>
      Внимание позволяет каждому элементу последовательности «смотреть» на все
      остальные и взвешивать их важность. Self-attention вычисляется через
      запросы Q, ключи K и значения V:
    </P>
    <Formula>Attention(Q, K, V) = softmax( Q·Kᵀ / √dₖ ) · V</Formula>
    <P>
      Скалярное произведение Q·Kᵀ измеряет «похожесть» элементов, softmax
      превращает её в веса, которыми усредняются значения V.
    </P>

    <H2>Почему это важно</H2>
    <Ul
      items={[
        "Параллелизм: вся последовательность обрабатывается сразу, а не по шагам, как в RNN.",
        "Длинный контекст: связи между далёкими элементами устанавливаются напрямую.",
        "Масштабируемость: качество растёт с размером модели и данных.",
      ]}
    />

    <H2>Из чего состоит</H2>
    <Ul
      items={[
        "Multi-head attention — несколько «голов» внимания одновременно.",
        "Позиционное кодирование — добавляет информацию о порядке элементов.",
        "Полносвязные подслои, нормализация и остаточные связи.",
      ]}
    />

    <Callout variant="tip">
      Encoder-архитектуры (BERT) хороши для понимания текста, decoder-архитектуры
      (GPT) — для генерации. Обе выросли из исходного трансформера.
    </Callout>

    <H2>Интуиция: на что «смотрит» слово</H2>
    <P>
      В фразе «банк у реки» модель внимания связывает «банк» с «рекой» и понижает
      вес финансового значения. Каждое слово формирует свой запрос (Q) и
      сравнивает его с ключами (K) остальных слов — так контекст уточняет смысл.
    </P>

    <H2>Зачем позиционное кодирование</H2>
    <P>
      Внимание само по себе не учитывает порядок — для него «кот поймал мышь» и
      «мышь поймала кота» одинаковы. Позиционное кодирование добавляет к каждому
      элементу информацию о его месте в последовательности.
    </P>

    <H2>BERT и GPT</H2>
    <Ul
      items={[
        "BERT (encoder): видит контекст с обеих сторон, силён в понимании — классификация, поиск, извлечение ответа.",
        "GPT (decoder): предсказывает следующий токен слева направо, силён в генерации текста.",
        "Обучение в два этапа: предобучение на огромных корпусах + дообучение под задачу.",
      ]}
    />

    <Callout variant="info">
      Большие языковые модели (LLM) — это трансформеры, масштабированные до
      миллиардов параметров. Их качество предсказуемо растёт с размером модели и
      объёмом данных.
    </Callout>

    <Sources refs={[REFS.transformer2017, REFS.goodfellow, REFS.nikolenko]} />
  </Article>
);

export default TransformersArticle;
