import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Formula,
  Callout,
  Term,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const NumericalOptimizationArticle: React.FC = () => (
  <Article>
    <Title>Численные методы оптимизации</Title>
    <Lead>
      Обучение нейросети — это задача минимизации функции потерь по параметрам.
      Аналитически решить её невозможно, поэтому применяют итерационные численные
      методы, основанные на градиенте.
    </Lead>

    <H2>Постановка задачи</H2>
    <P>
      Нужно найти такие параметры <Formula>θ</Formula> (веса и смещения), которые
      минимизируют <Term id="loss">функцию потерь</Term>{" "}
      <Formula>L(θ)</Formula> на обучающей выборке:
    </P>
    <Formula>θ* = arg min_θ L(θ)</Formula>
    <P>
      У реальных сетей <Formula>θ</Formula> — это миллионы параметров, а{" "}
      <Formula>L</Formula> — невыпуклая функция со множеством минимумов и седловых
      точек. Прямого решения нет, поэтому идём к минимуму малыми шагами.
    </P>

    <H2>Градиентный спуск</H2>
    <P>
      Базовый метод — <Term id="gradient-descent">градиентный спуск</Term>. На
      каждой итерации параметры сдвигаются против <Term id="gradient">градиента</Term>{" "}
      (в направлении наискорейшего убывания):
    </P>
    <Formula>θ ← θ − η · ∇L(θ)</Formula>
    <P>
      где <Term id="learning-rate">η</Term> — скорость обучения (длина шага).
      Слишком малый шаг — медленная сходимость; слишком большой — расходимость или
      колебания.
    </P>
    <H3>Варианты по объёму данных</H3>
    <Ul
      items={[
        "Пакетный (batch) спуск: градиент по всей выборке — точный, но дорогой.",
        "Стохастический (SGD): градиент по одному примеру — быстрый, но шумный.",
        "Мини-батчевый: компромисс — градиент по небольшой группе примеров; стандарт на практике.",
      ]}
    />
    <Callout variant="info">
      Стохастический шум в SGD не только ускоряет вычисления, но и помогает
      «выпрыгивать» из плохих локальных минимумов и седловых точек.
    </Callout>

    <H2>Где взять градиент</H2>
    <P>
      Градиент функции потерь по всем параметрам эффективно вычисляется алгоритмом
      обратного распространения ошибки. Он применяет правило цепочки к
      вычислительному графу сети, переиспользуя промежуточные результаты прямого
      прохода — это делает расчёт градиента сопоставимым по стоимости с одним
      прямым проходом.
    </P>
    <Formula>∂L/∂θ = (∂L/∂y) · (∂y/∂θ)</Formula>

    <H2>Методы с моментом и адаптивным шагом</H2>
    <P>
      Чистый SGD сходится медленно в «оврагах» функции потерь. Улучшения:
    </P>
    <H3>Момент (momentum)</H3>
    <P>
      Накапливает «скорость» движения, сглаживая колебания и ускоряя движение
      вдоль устойчивого направления:
    </P>
    <Formula>v ← β·v − η·∇L(θ); θ ← θ + v</Formula>
    <H3>Адаптивные методы</H3>
    <Ul
      items={[
        "AdaGrad — масштабирует шаг для каждого параметра по накопленной сумме квадратов градиентов.",
        "RMSProp — использует скользящее среднее квадратов градиентов, что решает проблему затухания шага в AdaGrad.",
        "Adam — объединяет идею момента и RMSProp; один из самых популярных оптимизаторов по умолчанию.",
      ]}
    />
    <Callout variant="tip">
      Adam часто берут как стартовый выбор: он устойчив к выбору скорости обучения
      и хорошо работает «из коробки» на большинстве задач.
    </Callout>

    <H2>Проблемы и приёмы</H2>
    <Ul
      items={[
        "Локальные минимумы и седловые точки: для глубоких сетей чаще мешают именно седловые точки, а не плохие минимумы.",
        "Исчезающий и взрывающийся градиент: лечится ReLU, нормализацией, остаточными связями, ограничением нормы градиента (clipping).",
        "Плато и овраги: помогают момент и адаптивные методы.",
        "Расписания скорости обучения (warmup, затухание) ускоряют и стабилизируют сходимость.",
      ]}
    />
    <Callout variant="warn">
      Невыпуклость означает, что глобальный минимум не гарантирован. На практике
      важен не идеальный минимум, а решение, которое хорошо обобщается на новые
      данные.
    </Callout>

    <H2>Условие остановки</H2>
    <Ul
      items={[
        "Норма градиента стала очень малой — параметры почти не меняются.",
        "Достигнуто максимальное число эпох.",
        "Ошибка на валидации перестала убывать (ранняя остановка против переобучения).",
      ]}
    />

    <Sources refs={[REFS.goodfellow, REFS.nikolenko, REFS.rumelhart1986]} />
  </Article>
);

export default NumericalOptimizationArticle;
