import React from "react";
import {
  Article,
  Title,
  Lead,
  H2,
  H3,
  P,
  Ul,
  Callout,
  CodeBlock,
  InlineCode,
  Sources,
} from "../components/ArticleKit";
import { REFS } from "../references";

const OnnxArticle: React.FC = () => (
  <Article>
    <Title>ONNX — обмен моделями</Title>
    <Lead>
      ONNX (Open Neural Network Exchange) — открытый формат для представления
      моделей машинного обучения. Его цель — сделать модели переносимыми: обучить
      в одном фреймворке (например, PyTorch), а запускать в другом окружении без
      переписывания кода.
    </Lead>

    <H2>Зачем нужен общий формат</H2>
    <P>
      Разные фреймворки хранят модели по-своему, и перенести обученную сеть из
      одного в другой бывает непросто. ONNX задаёт единое описание модели,
      которое понимают многие инструменты обучения и среды исполнения.
    </P>
    <Ul
      items={[
        "Разделяет обучение и развёртывание (inference): обучаем в одном месте, запускаем в другом.",
        "Снижает привязку к одному фреймворку (vendor lock-in).",
        "Упрощает оптимизацию под конкретное «железо» — CPU, GPU, мобильные и встраиваемые устройства.",
      ]}
    />

    <H2>Как устроена модель ONNX</H2>
    <P>
      Модель ONNX — это вычислительный граф. Узлы графа — операторы (свёртка,
      матричное умножение, активации), рёбра — потоки тензоров между ними. Веса
      хранятся как инициализаторы.
    </P>
    <Ul
      items={[
        "Граф операторов с описанием входов и выходов.",
        "Стандартный набор операторов (opset) фиксированной версии.",
        "Метаданные: имена входов/выходов, формы и типы тензоров.",
      ]}
    />
    <Callout variant="info">
      Версия набора операторов (<InlineCode>opset</InlineCode>) важна: среда
      исполнения должна поддерживать ту версию, в которую экспортирована модель.
    </Callout>

    <H2>Экспорт из PyTorch</H2>
    <P>
      PyTorch умеет экспортировать модель в ONNX «из коробки». Для этого нужен
      пример входного тензора, по которому трассируется граф вычислений.
    </P>
    <CodeBlock
      language="python"
      code={`import torch

model.eval()
dummy = torch.randn(1, 3, 224, 224)   # пример входа нужной формы

torch.onnx.export(
    model, dummy, "model.onnx",
    input_names=["input"],
    output_names=["output"],
    dynamic_axes={"input": {0: "batch"}, "output": {0: "batch"}},
    opset_version=17,
)`}
    />

    <H2>Запуск через ONNX Runtime</H2>
    <P>
      Для инференса обычно используют ONNX Runtime — кроссплатформенный движок,
      который оптимизирует граф и исполняет его на доступном оборудовании.
    </P>
    <CodeBlock
      language="python"
      code={`import onnxruntime as ort
import numpy as np

sess = ort.InferenceSession("model.onnx")

x = np.random.randn(1, 3, 224, 224).astype(np.float32)
outputs = sess.run(None, {"input": x})
print(outputs[0].shape)`}
    />

    <H2>Где это полезно</H2>
    <H3>Сценарии применения</H3>
    <Ul
      items={[
        "Развёртывание на серверах с минимальными зависимостями.",
        "Запуск на мобильных и встраиваемых устройствах.",
        "Инференс в браузере и на разнородном оборудовании.",
        "Применение оптимизаций: квантизация, слияние операторов.",
      ]}
    />
    <H3>Ограничения</H3>
    <Ul
      items={[
        "Нестандартные или новые слои могут не иметь оператора в нужном opset.",
        "Динамические по структуре графы (сложные ветвления) экспортируются труднее.",
        "После экспорта стоит сверять выходы ONNX и исходной модели на тестовых данных.",
      ]}
    />
    <Callout variant="tip">
      Типичный путь: обучить модель в PyTorch или TensorFlow, экспортировать в
      ONNX и развернуть через ONNX Runtime — так разработка и продакшен остаются
      независимыми.
    </Callout>

    <Sources refs={[REFS.pytorch, REFS.tensorflow, REFS.nikolenko]} />
  </Article>
);

export default OnnxArticle;
